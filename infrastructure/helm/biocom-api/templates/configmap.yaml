apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "biocom-api.fullname" . }}-config
  labels:
    {{- include "biocom-api.labels" . | nindent 4 }}
data:
  # 설정 파일이나 환경 변수를 여기에 추가할 수 있습니다
  # 예시:
  # application.properties: |
  #   server.port=3000
  #   logging.level=info