# Base Kustomization - biocom-bo-api 공통 리소스 정의

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 공통 리소스
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

# 공통 레이블
commonLabels:
  app: biocom-bo-api
  managed-by: kustomize
