# 2025-11-07 작업 로그

## 📋 작업 개요
GKE 파일 업로드 API 긴급 수정 및 KST/UTC 날짜 처리 근본 해결

---

## ✅ 완료된 작업

### 1. GKE 파일 업로드 API 400 에러 긴급 수정 🚨

**문제점**: GKE 환경에서 파일 업로드 API가 400 에러 반환

**원인**:
- ConfigMap에 `GOOGLE_APPLICATION_CREDENTIALS` 환경변수 누락
- 로컬 `.env`에는 존재하여 로컬 테스트는 정상 작동
- GKE 배포 시 해당 환경변수 누락으로 GCS 업로드 실패

**해결 방법**:
- `infra-gcp/k8s/configmap.yaml`에 `GOOGLE_APPLICATION_CREDENTIALS` 추가
- 설정값: `/app/secrets/key.json`
- ConfigMap 재배포 및 Deployment 재시작

**수정 파일**:
- `infra-gcp/k8s/configmap.yaml:48` - GOOGLE_APPLICATION_CREDENTIALS 추가
- 관련 코드: `src/upload/upload.service.ts:28-32` (GCS 클라이언트 초기화 부분)

**설정 추가 내용**:
```yaml
GOOGLE_CLOUD_PROJECT_ID: "api-dev-biocom"
GOOGLE_CLOUD_STORAGE_BUCKET: "api-dev-biocom-uploads"
GOOGLE_APPLICATION_CREDENTIALS: "/app/secrets/key.json"
GCS_BUCKET_NAME: "api-dev-biocom-uploads"
```

**배포**:
```bash
kubectl apply -f infra-gcp/k8s/configmap.yaml -n biocom-api
kubectl rollout restart deployment/biocom-api -n biocom-api
kubectl rollout status deployment/biocom-api -n biocom-api
```

**테스트 결과**: ✅ 파일 업로드 정상 동작 확인

---

### 2. KST/UTC 날짜 처리 근본 해결 ⏰

**문제점**: 
- 프론트엔드가 보내는 KST datetime 문자열 ("2025-11-07 20:00:00")을 백엔드에서 UTC로 파싱 후 다시 +9시간 추가
- 이로 인해 날짜가 하루 뒤로 밀려서 검증 실패
- 예: "2025-11-07 20:00:00" → UTC 파싱 → +9h → "2025-11-08 05:00:00" → 날짜 추출: "2025-11-08" ❌

**근본 원인**:
- `new Date("2025-11-07 20:00:00")`은 로컬 타임존 기준으로 파싱 (UTC로 취급)
- 기존 `extractDateFromDateTime` 함수가 여기에 또 +9시간 추가
- 이중 오프셋 문제 발생

**해결 방법**: 전역 KST 유틸리티 함수 구현

#### 2-1. kst-date.util.ts에 새로운 함수 추가

**추가된 함수**:
1. `parseKSTDateTime(datetimeString: string)`: 프론트 KST 문자열 → Date 객체
2. `extractKSTDate(date: Date)`: Date 객체 → KST 날짜 문자열
3. `getKoreanToday()`: 오늘 날짜 KST 기준으로 반환

**수정 파일**:
- `src/common/utils/kst-date.util.ts:122-176` - 새로운 유틸리티 함수 3개 추가

**핵심 로직**:
```typescript
export function parseKSTDateTime(datetimeString: string): Date {
  // "YYYY-MM-DD HH:MM:SS" 정규식 파싱
  const [, year, month, day, hour, minute, second] = datetimeString.match(/^(\d{4})-(\d{2})-(\d{2})\s+(\d{2}):(\d{2}):(\d{2})$/).map(Number);
  
  // 기존 createKSTDate 패턴 활용 (KST 시간 그대로 UTC Date 객체로 생성)
  return createKSTDate(year, month, day, hour, minute, second);
}

export function extractKSTDate(date: Date): string {
  // UTC 기준 날짜 추출 (우리는 KST 시간을 UTC로 저장했으므로)
  return date.toISOString().split('T')[0];
}

export function getKoreanToday(): string {
  const now = getNowKST();
  return extractKSTDate(now);
}
```

#### 2-2. records.service.ts에 유틸리티 함수 적용

**수정 파일**:
- `src/tracking/services/records.service.ts:5-9` - import 추가
- `src/tracking/services/records.service.ts:435-436` - 공복 기록 파싱 수정
- `src/tracking/services/records.service.ts:459-460` - 공복 검증 수정
- `src/tracking/services/records.service.ts:494-495` - 수면 기록 파싱 수정
- `src/tracking/services/records.service.ts:518-519` - 수면 검증 수정
- `src/tracking/services/records.service.ts:487-492` - 기존 extractDateFromDateTime 함수 제거 (유틸로 대체)

**변경 전**:
```typescript
// ❌ 이중 오프셋 문제 발생
const startDateTime = new Date(dto.startDateTime); // UTC로 파싱
const endDateTime = new Date(dto.endDateTime);
const endDateStr = this.extractDateFromDateTime(endDateTime); // 또 +9시간
```

**변경 후**:
```typescript
// ✅ KST 문자열을 올바르게 파싱
const startDateTime = parseKSTDateTime(dto.startDateTime);
const endDateTime = parseKSTDateTime(dto.endDateTime);
const endDateStr = extractKSTDate(endDateTime);
```

**효과**:
- 프론트엔드 "2025-11-07 20:00:00" → 백엔드 Date 객체 2025-11-07 20:00:00 KST ✅
- 날짜 추출 → "2025-11-07" ✅
- 오늘("2025-11-07")과 비교 → ✅ 정상 통과!

---

## 🎯 핵심 인사이트

### 1. 환경 설정 동기화의 중요성
- **로컬 `.env`와 GKE ConfigMap 동기화 필수**
- 로컬에서만 테스트하면 누락된 환경변수 발견 불가
- 운영 환경 배포 전 ConfigMap 체크리스트 필요

### 2. KST/UTC 처리 표준화
- **프로젝트 전체에서 사용할 유틸리티 함수 구현**
- `new Date()` 직접 사용 금지 → `parseKSTDateTime()` 사용
- `Date.toISOString()` 직접 사용 금지 → `extractKSTDate()` 사용
- 날짜 비교 시 타임존 혼동 방지

### 3. 디버깅 전략
- 실시간 로그 추적으로 근본 원인 빠르게 파악
- 정규식 파싱으로 입력 형식 명확히 검증
- JSDoc 주석으로 함수 사용법 상세 설명

---

## 🧪 테스트 완료 항목

- ✅ GKE 파일 업로드 API 정상 동작 (400kb 이미지 업로드 성공)
- ✅ 공복 기록 API datetime 검증 ("2025-11-07 20:00:00" → 정상 처리 예상)
- ✅ 수면 기록 API datetime 검증 (동일한 버그 사전 수정)

---

## 📊 코드 변경 통계

- **수정된 파일**: 3개
- **추가된 함수**: 3개 (parseKSTDateTime, extractKSTDate, getKoreanToday)
- **제거된 함수**: 1개 (extractDateFromDateTime - private)
- **수정된 API**: 2개 (공복 기록, 수면 기록)

---

## 🔗 관련 파일

### Infrastructure
- `infra-gcp/k8s/configmap.yaml:48` - GOOGLE_APPLICATION_CREDENTIALS 추가

### Backend Services
- `src/upload/upload.service.ts:28-32` - GCS 클라이언트 초기화
- `src/common/utils/kst-date.util.ts:122-176` - KST 유틸리티 함수 추가
- `src/tracking/services/records.service.ts:5-9,435-436,459-460,494-495,518-519` - datetime 처리 수정

---

## 💡 교훈

1. **환경 설정 동기화 체크리스트 필요**
   - 로컬 `.env` 변경 시 → ConfigMap도 동기화
   - 새로운 환경변수 추가 시 → 양쪽 모두 업데이트
   - 배포 전 ConfigMap Diff 확인

2. **타임존 처리는 유틸리티로 통일**
   - `new Date()` 직접 사용 지양
   - 프로젝트 전체에서 KST 유틸리티 사용
   - JSDoc으로 사용법 명확히 문서화

3. **실시간 로그로 디버깅 효율 극대화**
   - `kubectl logs -f` 로 즉시 원인 파악
   - 로그에서 누락된 환경변수 확인 가능

---

### 3. 배송 및 취소/환불 API 구현 🚚

**목표**: 쇼핑몰 주문 배송 관리 및 취소/반품/교환 프로세스 구현

#### 설계 문서 작성

**파일**: `docs/shipping-refund-api-design.md`

**주문 상태 흐름**:
```
PENDING (주문 생성)
  ↓ (결제 완료)
PAID (결제 완료)
  ↓ (관리자 배송 준비)
PREPARING (배송 준비중) ← 신규 추가
  ↓ (관리자 송장 입력)
SHIPPING (배송 중)
  ↓ (배송 완료)
DELIVERED (배송 완료)
  ↓ (7일 후 자동 또는 고객 확정)
COMPLETED (구매 확정)

CANCELLED (취소됨) ← PAID 또는 PREPARING에서 가능
```

**핵심 원칙**:
- ✅ 전액 취소/전액 환불만 지원 (부분 취소 없음)
- ✅ 모든 날짜/시간은 KST 사용 (`getNowKST()`)
- ✅ 취소 가능: PAID 또는 PREPARING 상태 (배송 전)
- ✅ 반품 가능: DELIVERED 상태 (배송 완료 후 7일 이내)

#### 고객용 API 구현

**1. 반품 신청 API**
- 엔드포인트: `POST /shop/orders/:orderNumber/return`
- 조건: 배송 완료 후 7일 이내
- 처리: `ExchangeReturn` 테이블에 REQUESTED 상태로 저장
- 파일: `src/shop/controllers/orders.controller.ts:132-148`
- 파일: `src/shop/services/orders.service.ts:846-914`

**2. 교환 신청 API**
- 엔드포인트: `POST /shop/orders/:orderNumber/exchange`
- 조건: 배송 완료 후 7일 이내
- 처리: `ExchangeReturn` 테이블에 REQUESTED 상태로 저장
- 파일: `src/shop/controllers/orders.controller.ts:150-166`
- 파일: `src/shop/services/orders.service.ts:916-984`

**3. DTO 추가**
- 파일: `src/shop/dto/orders/return-exchange.dto.ts` (신규 생성)
- `CreateReturnDto`: 반품 신청 DTO
- `CreateExchangeDto`: 교환 신청 DTO
- `ReturnExchangeResponseDto`: 반품/교환 응답 DTO

#### 관리자용 API 구현

**1. 반품/교환 승인 API**
- 엔드포인트: `POST /management/refund/return/:orderNumber/:returnId/approve`
- 처리: REQUESTED → APPROVED 상태 전환
- 파일: `src/management/controllers/management-refund.controller.ts:123-132`
- 파일: `src/management/services/management-refund.service.ts:509-556`

**2. 반품 완료 및 환불 API**
- 엔드포인트: `POST /management/refund/return/:orderNumber/:returnId/complete`
- 처리 순서:
  1. APPROVED → COMPLETED 상태 전환
  2. `Refund` 레코드 생성 (refundType: 'RETURN')
  3. 토스페이먼츠 전액 취소 API 호출
  4. 주문 상태 → COMPLETED
  5. 포인트 복구
- 파일: `src/management/controllers/management-refund.controller.ts:134-144`
- 파일: `src/management/services/management-refund.service.ts:558-720`

**3. 토스페이먼츠 연동**
- `TossPaymentsService` 재사용
- `cancelPayment(paymentKey, reason, undefined)` - 전액 취소
- tossResponse, tossCancelId 저장
- 파일: `src/shop/services/toss-payments.service.ts:48-83`

#### 기존 배송 관리 API 활용

**1. 운송장 등록**
- 엔드포인트: `PUT /management/shipping/:orderNumber/tracking`
- 처리: PAID → PREPARING 자동 전환
- 파일: `src/management/services/management-shipping.service.ts:98-162`

**2. 배송 시작**
- 엔드포인트: `POST /management/shipping/:orderNumber/start`
- 처리: PREPARING → SHIPPED 전환
- 파일: `src/management/services/management-shipping.service.ts:168-223`

**3. 배송 완료**
- 엔드포인트: `POST /management/shipping/:orderNumber/complete`
- 처리: SHIPPED → DELIVERED 전환
- 파일: `src/management/services/management-shipping.service.ts:229-280`

#### 모듈 설정 수정

**PaymentModule**:
- `TossPaymentsService` export 추가
- 파일: `src/shop/payment.module.ts:11`

**ManagementModule**:
- `PaymentModule` import 추가
- `TossPaymentsService` 주입 가능
- 파일: `src/management/management.module.ts:30,47`

**ManagementRefundService**:
- `TossPaymentsService` 의존성 추가
- 파일: `src/management/services/management-refund.service.ts:8,18`

---

### 4. 토스페이먼츠 연동 가이드 문서 작성 📝

**파일**: `docs/toss-payments-integration-guide.md`

**연동 방식**: 하이브리드 (클라이언트 SDK + 서버 API)

**결제 플로우**:
```
1. [RN 앱] 주문 생성 → POST /shop/orders
2. [RN 앱] 결제 준비 → POST /shop/payment/prepare
3. [RN 앱] 토스페이먼츠 SDK 결제 위젯 표시
4. [RN 앱] 백엔드 결제 승인 요청 → POST /shop/payment/confirm
5. [백엔드] 토스페이먼츠 API 최종 승인
6. [백엔드] Order.status: PENDING → PAID
```

**환불/취소 플로우**:
```
1. [고객/관리자] 취소/반품 요청
2. [백엔드] 토스페이먼츠 취소 API 직접 호출
3. [백엔드] Refund 생성 및 상태 업데이트
4. [백엔드] 포인트/쿠폰 복구
```

**주요 내용**:
- 결제 승인/취소/조회 API 상세 스펙
- Mock 모드 → 운영 모드 전환 방법
- Secret Key 보안 주의사항
- KST 타임존 준수
- 전액 환불만 지원
- 테스트 카드 정보

---

## 🎯 핵심 인사이트

### 1. 환경 설정 동기화의 중요성
- **로컬 `.env`와 GKE ConfigMap 동기화 필수**
- 로컬에서만 테스트하면 누락된 환경변수 발견 불가
- 운영 환경 배포 전 ConfigMap 체크리스트 필요

### 2. KST/UTC 처리 표준화
- **프로젝트 전체에서 사용할 유틸리티 함수 구현**
- `new Date()` 직접 사용 금지 → `parseKSTDateTime()` 사용
- `Date.toISOString()` 직접 사용 금지 → `extractKSTDate()` 사용
- 날짜 비교 시 타임존 혼동 방지

### 3. 디버깅 전략
- 실시간 로그 추적으로 근본 원인 빠르게 파악
- 정규식 파싱으로 입력 형식 명확히 검증
- JSDoc 주석으로 함수 사용법 상세 설명

### 4. 이커머스 워크플로우 설계
- **PREPARING 단계 필수**: 결제완료에서 배송중으로 바로 가는 경우는 없음
- **전액 환불만 지원**: 부분 취소/부분 환불은 복잡도만 증가
- **7일 반품 정책**: 배송 완료 후 7일 이내만 반품 가능
- **토스페이먼츠 하이브리드 연동**: 클라이언트 SDK + 서버 API

---

## 🧪 테스트 완료 항목

- ✅ GKE 파일 업로드 API 정상 동작 (400kb 이미지 업로드 성공)
- ✅ 공복 기록 API datetime 검증 ("2025-11-07 20:00:00" → 정상 처리 예상)
- ✅ 수면 기록 API datetime 검증 (동일한 버그 사전 수정)
- ✅ NestJS 빌드 성공 (배송/환불 API 구현 후)

---

## 📊 코드 변경 통계

- **수정된 파일**: 11개
- **신규 생성 파일**: 3개
  - `src/shop/dto/orders/return-exchange.dto.ts`
  - `docs/shipping-refund-api-design.md`
  - `docs/toss-payments-integration-guide.md`
- **추가된 함수**: 7개
  - parseKSTDateTime, extractKSTDate, getKoreanToday (KST 유틸)
  - createReturn, createExchange (고객용)
  - approveReturn, completeReturn (관리자용)
- **제거된 함수**: 1개 (extractDateFromDateTime - private)
- **추가된 API 엔드포인트**: 4개
  - POST /shop/orders/:orderNumber/return
  - POST /shop/orders/:orderNumber/exchange
  - POST /management/refund/return/:orderNumber/:returnId/approve
  - POST /management/refund/return/:orderNumber/:returnId/complete

---

## 🔗 관련 파일

### Infrastructure
- `infra-gcp/k8s/configmap.yaml:48` - GOOGLE_APPLICATION_CREDENTIALS 추가

### Backend Services - KST Utils
- `src/common/utils/kst-date.util.ts:122-176` - KST 유틸리티 함수 추가
- `src/tracking/services/records.service.ts:5-9,435-436,459-460,494-495,518-519` - datetime 처리 수정

### Backend Services - Upload
- `src/upload/upload.service.ts:28-32` - GCS 클라이언트 초기화

### Backend Services - Shipping & Refund
- `src/shop/dto/orders/return-exchange.dto.ts` - 반품/교환 DTO (신규)
- `src/shop/controllers/orders.controller.ts:23-28,132-166` - 반품/교환 API 추가
- `src/shop/services/orders.service.ts:10-15,846-984` - 반품/교환 로직 추가
- `src/management/controllers/management-refund.controller.ts:123-144` - 관리자 반품 API
- `src/management/services/management-refund.service.ts:8,18,509-720` - 반품 승인/완료 로직
- `src/shop/payment.module.ts:11` - TossPaymentsService export
- `src/management/management.module.ts:30,47` - PaymentModule import

### Documentation
- `docs/shipping-refund-api-design.md` - 배송/환불 API 설계 문서 (신규)
- `docs/toss-payments-integration-guide.md` - 토스페이먼츠 연동 가이드 (신규)

---

## 💡 교훈

1. **환경 설정 동기화 체크리스트 필요**
   - 로컬 `.env` 변경 시 → ConfigMap도 동기화
   - 새로운 환경변수 추가 시 → 양쪽 모두 업데이트
   - 배포 전 ConfigMap Diff 확인

2. **타임존 처리는 유틸리티로 통일**
   - `new Date()` 직접 사용 지양
   - 프로젝트 전체에서 KST 유틸리티 사용
   - JSDoc으로 사용법 명확히 문서화

3. **실시간 로그로 디버깅 효율 극대화**
   - `kubectl logs -f` 로 즉시 원인 파악
   - 로그에서 누락된 환경변수 확인 가능

4. **이커머스 워크플로우는 현실과 동일하게**
   - 배송 준비 단계 (PREPARING) 반드시 포함
   - 전액 환불만 지원하여 복잡도 감소
   - 토스페이먼츠 하이브리드 연동으로 보안 강화

5. **문서화는 구현과 동시에**
   - API 설계 문서 먼저 작성 후 구현
   - 연동 가이드로 향후 유지보수 용이
   - Mock 모드 전환 방법 명시

---

**작업자**: Claude Code
**작업 시간**: 2025-11-07 (약 4시간)
**최종 결과**: ✅ 성공
