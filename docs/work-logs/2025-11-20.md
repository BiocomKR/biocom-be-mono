# 작업일지 2025-11-20 (수)

## 📋 작업 요약

### 1. 기록/통계 API 전면 개편 ⭐️⭐️⭐️
**목적**: NEWCOMER 사용자 UX 개선 및 API 응답 표준화

#### 1-1. NEWCOMER 접근 정책 개선
- **기존**: NEWCOMER 사용자 API 접근 차단
- **변경**: 모든 사용자 접근 허용, NEWCOMER는 예시 데이터 제공
- **핵심 로직**: 챌린지 이력 체크
  - user_challenges 테이블에 레코드 있음 → 과거 챌린저 → 실제 데이터
  - user_challenges 테이블에 레코드 없음 → 완전 신규 → 예시 데이터
- **수정 파일**: `src/tracking/guards/record-access.guard.ts`

#### 1-2. 기록 API 개선 (`/api/tracking/records`)
- **6개 recordType 전체 표시** (데이터 없어도 null로 표시)
  - 고정 순서: BEAUTY → DIET → SUPPLEMENT → FASTING → SLEEP → ACTIVITY
- **필드 변경**:
  - `total` 필드 제거
  - `isReal` 필드 추가 (실제 데이터: true, 예시 데이터: false)
- **NEWCOMER용 예시 데이터 생성 함수 추가**: `getSampleRecords()`
- **수정 파일**:
  - `src/tracking/services/records.service.ts`
  - `src/tracking/controllers/records.controller.ts`

#### 1-3. 통계 API 개선 (`/api/tracking/statistics/summary`)
- **필드 정리**:
  - `comment`, `totalComment`, `message` 필드 제거
  - `isReal` 필드 추가
- **NEWCOMER용 예시 통계 데이터 생성**: `getSampleStatisticsSummary()`
- **supplement 섹션**: `{ summary: {}, detailData: {} }` (기획 미확정으로 임시 처리)
- **수정 파일**:
  - `src/tracking/services/statistics.service.ts`
  - `src/tracking/controllers/statistics.controller.ts`

---

### 2. 지연성알러지 검색 로직 개선
- **기존**: 신규 지연성알러지(D0060)만 검색
- **변경**: 신규(D0060) + 구버전(D0004) 통합 검색
- **180일 체크 정책 폐지**: 무제한 접근으로 변경
- **수정 파일**: `src/tracking/services/records.service.ts:1224`

---

### 3. Prisma Schema 수정
#### 3-1. UserChart 테이블 수정
- **문제**: `updated_at` 컬럼 NOT NULL 제약으로 생성 시 오류
- **해결**: `updatedAt DateTime?`로 nullable 처리
- **DB 쿼리**: `ALTER TABLE user_charts ALTER COLUMN updated_at DROP NOT NULL;`

#### 3-2. PushNotificationLog 모델 추가
- **목적**: 푸시 알림 발송 로그 테이블 스키마 추가
- **테이블**: `push_notification_logs` (14개 레코드 존재)
- **주요 필드**: user_id, push_token_id, title, body, success, sent_at, read_at, clicked_at 등

---

### 4. 홈 서비스 성능 개선
- **변경**: 차트 데이터 조회/저장 로직을 백그라운드 실행으로 변경
- **효과**: 홈 화면 응답 속도 개선
- **기존**:
  ```typescript
  await this.fetchAndSaveChartData(userId, user.mobile);
  ```
- **변경**:
  ```typescript
  this.fetchAndSaveChartData(userId, user.mobile).catch(err => {
    this.logger.warn(`차트 데이터 백그라운드 저장 실패 - 사용자 ID: ${userId}`, err);
  });
  ```
- **수정 파일**: `src/home/home.service.ts:69-72`

---

### 5. 외부 커밋 통합 (Git Pull)
#### 5-1. Firebase 키 gitignore 추가 (`82dd6de`)
- `.gitignore`에 `*firebase*.json` 추가

#### 5-2. 푸시 알림 스케줄 및 캠페인 시스템 추가 (`c210a1d`)
- **PushToken 모델 개선**:
  - User, PushNotificationLog와 Relation 추가
  - 주석 추가 (한글 설명)

- **PushNotificationLog 모델 개선**:
  - User, PushToken, Campaign과 Relation 추가
  - 인덱스 추가 (campaignId, pushTokenId, type, success, isTest)

- **PushNotificationSchedule 모델 추가**:
  - 반복 발송 설정 및 메시지 템플릿 관리
  - ONCE(단발성) / RECURRING(반복) 타입 지원
  - 크론 표현식, 타겟팅 쿼리 지원

- **PushNotificationCampaign 모델 추가**:
  - 특정 시점의 실제 발송 실행 기록
  - MANUAL / SCHEDULED / RECURRING 타입
  - 상태 관리 (PENDING, PROCESSING, COMPLETED, FAILED, CANCELLED)
  - 발송 통계 (targetCount, sentCount, failCount)

---

## 🚀 배포 및 동기화
- ✅ Prisma 클라이언트 재생성 (3회)
- ✅ Prisma DB 푸시 (2회)
- ✅ Git 커밋 및 푸시 (`49aa6a3`)
- ✅ Git Pull 및 스키마 동기화 (`82dd6de`)

---

## 📊 작업 통계
- **수정 파일**: 9개
- **추가 라인**: 약 600줄
- **삭제 라인**: 약 80줄
- **커밋**: 1개 (feat: NEWCOMER 사용자 접근 정책 개선 및 기록/통계 API 전면 개편)
- **통합 커밋**: 2개 (외부)

---

## 💡 주요 의사결정
1. **NEWCOMER 접근 정책**: 차단 → 예시 데이터 제공으로 전환 (UX 개선)
2. **챌린지 이력 판단**: user_challenges 테이블 레코드 존재 여부로 판단
3. **180일 정책 폐지**: 지연성알러지 검사 결과 무제한 접근
4. **백그라운드 처리**: 차트 데이터 조회를 비동기로 처리하여 응답 속도 개선
5. **필드 정리**: 불필요한 comment, totalComment 필드 제거

---

## 🔧 기술 스택
- NestJS
- Prisma ORM
- PostgreSQL (GCP Cloud SQL)
- TypeScript

---

## 📝 특이사항
- Prisma Schema 대폭 개편 (푸시 알림 시스템 추가, 317줄 추가)
- user_charts 테이블 updated_at 컬럼 NULL 허용 (DB 직접 수정)
- home.service.ts의 차트 데이터 조회 로직 성능 개선

---

**작업자**: Claude Code (AI Assistant)
**작업 시간**: 2025-11-20 오후
**브랜치**: development
