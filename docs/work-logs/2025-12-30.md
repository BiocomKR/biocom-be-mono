# 작업 로그 - 2025-12-30

## 1. 퀴즈 포인트 버그 수정

### 문제
- 퀴즈에서 오답을 선택해도 포인트(200점)가 지급되는 버그 발견
- user_id=42가 quiz_id=64, 65에서 오답인데도 각 200점씩 받음

### 원인
- `quiz-completion.service.ts` 485라인에서 `isCorrect` 조건 누락
```typescript
// Before (버그)
const pointsEarned = (canEarnPoints && !pointsAlreadyGiven) ? basePoints : 0;

// After (수정)
const pointsEarned = (isCorrect && canEarnPoints && !pointsAlreadyGiven) ? basePoints : 0;
```

### 수정
- 파일: `src/quiz/quiz-completion.service.ts:485`
- 정답일 때만 포인트 지급되도록 `isCorrect &&` 조건 추가

---

## 2. GitHub Actions CI/CD 설정

### 워크플로우 구성
- **deploy-dev.yml**: development 브랜치 push → 개발서버 자동 배포
- **deploy-prod.yml**: main 브랜치 push → 운영서버 자동 배포

### 운영 배포 실패 수정
- **문제**: GKE 클러스터명 오류 (`ai-agent-cluster` → 존재하지 않음)
- **수정**: `.github/workflows/deploy-prod.yml`에서 클러스터명을 `biocom-cluster-prod`로 변경

### 배포 정책
- development push → 개발서버 자동 배포
- main merge (development에서) → 운영서버 배포 (반자동)
- main 브랜치는 직접 push 금지 (브랜치 보호 설정 필요)

---

## 3. 밸런스게임 포인트 지급 확인

### 상태
- 정상 작동 확인
- 중복 지급 방지 로직: `point_histories` 테이블에서 `relatedType='BALANCE_GAME'` + `relatedId=gameId` + 오늘 날짜로 체크
- 이미 오늘 포인트를 받은 경우 재지급되지 않음 (정상 동작)

---

## 4. 운영 배포 완료

### 배포 정보
- 이미지 태그: `20251230024308`
- Pod 상태: 3개 모두 Running (1/1 Ready)
- 배포 방식: GitHub Actions (main 브랜치 merge)

### 배포된 변경사항
- 퀴즈 포인트 버그 수정 (오답 시 포인트 미지급)
- 밸런스게임 포인트 지급 로직

---

## 5. solution 파일 업데이트 (git pull)

### 변경 파일
- `src/solution/dto/solution.dto.ts`
- `src/solution/solution.service.ts`

---

## 6. 플레이오토 배송 상태 동기화 스케줄러 확인

### 구현 현황 (이미 완료됨)
플레이오토 배송 상태 폴링 시스템 전체 구현 확인

### 구성 요소

| 구성요소 | 파일 | 역할 |
|---------|------|------|
| API 연동 | `src/playauto/providers/playauto.provider.ts` | 플레이오토 API 호출 |
| 주문 조회 | `getOrdersList()` | POST /orders 벌크 조회 |
| 상태 매핑 | `src/playauto/enums/playauto-status.enum.ts` | 플레이오토 → DB 상태 변환 |
| 스케줄러 | `src/playauto/services/shipping-sync-scheduler.service.ts` | 배치 실행 |
| MQ 워커 | `biocom-mq/src/processors/order-sync.processor.ts` | DB 업데이트 처리 |
| K8s CronJob | `infra-gcp/k8s/base/cronjob-sync-shipping-status.yaml` | 30분마다 실행 |

### 변경사항
- CronJob 스케줄 변경: `"0 * * * *"` → `"*/30 * * * *"` (1시간 → 30분 간격)

### 동작 흐름
```
[K8s CronJob] 30분마다 실행
    ↓
[biocom-api] ShippingSyncSchedulerService.handleShippingSync()
    ↓
1. DB에서 동기화 대상 주문 조회 (status NOT IN 완료/취소)
2. 플레이오토 벌크 API 호출 (POST /orders, 최근 30일)
3. logisticsUniq 기준 매칭
4. MQ Job 전송 (order-sync 큐)
    ↓
[biocom-mq] OrderSyncProcessor
    ↓
5. Order 테이블 업데이트 (status, shippedAt, deliveredAt)
6. Shipping 테이블 업데이트 (trackingNumber, courierName, status)
7. OrderStateLog 기록
```

### 상태 매핑
| 플레이오토 상태 | Order.status | Shipping.status |
|----------------|--------------|-----------------|
| 신규주문, 결제완료, 출고대기 | PAID | PENDING |
| 운송장출력 | SHIPPING | READY_FOR_SHIPMENT |
| 출고완료, 배송중 | SHIPPING | IN_TRANSIT |
| 배송완료 | DELIVERED | DELIVERED |
| 구매결정, 판매완료 | COMPLETED | DELIVERED |

---

## 7. 쇼핑몰 마무리 작업 현황

### 이번 버전 제외 (패스)
- 재고 관리 (시스템 준비 안됨)
- 결제/배송 알림 푸시 (다음 버전)

### 검증 필요
- 쿠폰 적용/복구 테스트
- 송장 등록 후 취소 → CANCEL_REQUESTED 플로우
- 관리자 환불 승인 플로우 (biocom-bo-api)
- 운영 환경 테스트 (토스 실결제, 플레이오토 운영샵, 웹훅)

---

## 8. 푸시 수신 동의 컬럼 추가

### 변경사항
- User 테이블에 `push_enabled` 컬럼 추가
- 타입: `Boolean?` (nullable, default null)
- 용도: 앱 사용자의 푸시 알림 수신 동의 여부

### 파일
- `prisma/schema.prisma`

---

## 9. 회원탈퇴 API 구현

### 구현 내용

#### 신규 테이블
- `withdrawn_users`: 탈퇴 회원 정보 보관 테이블
  - userId, name(암호화), mobile(암호화), reason, withdrawnAt

#### API 엔드포인트
- `POST /users/me/withdraw`
- Body: `{ reason?: string }` (탈퇴 사유, 선택)

#### 처리 로직
1. `withdrawn_users` 테이블에 정보 이관 (이름, 휴대폰 - 암호화 상태)
2. `users` 테이블 개인정보 마스킹:
   - name → `탈퇴회원_{userId}`
   - mobile → `deleted_{userId}`
   - email, password, billingKey, customerKey, 환불계좌 정보 → null
3. `is_active` → false, `deleted_at` → 현재시간
4. RefreshToken, PushToken 삭제 (로그아웃 처리)

#### 파일
- `prisma/schema.prisma` (WithdrawnUser 모델)
- `src/users/dto/withdraw.dto.ts`
- `src/users/mypage.service.ts` (withdraw 메서드)
- `src/users/mypage.controller.ts` (엔드포인트)

### is_active 전수조사 결과
- 로그인 시점(이메일/휴대폰)에서 `isActive`, `deletedAt` 체크 **이미 구현되어 있음**
- 탈퇴 회원은 로그인 불가 → 다른 API 접근 원천 차단
- **추가 수정 불필요**
