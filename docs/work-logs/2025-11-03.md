# 2025-11-03 작업일지

## 작업 내용

### 1. Beauty 기록 API 수정
- **파일**: `src/tracking/dto/records/records.dto.ts`, `src/tracking/services/records.service.ts`
- **변경사항**:
  - `CreateBeautyRecordDto`에서 `date` 파라미터 제거
  - 항상 한국 시간 오늘 날짜(`getKoreanToday()`)로 자동 저장
  - 점수 범위 변경: 1~5점 → 25, 20, 15, 10, 5점
  - Swagger 예시 점수 업데이트

### 2. DB 스키마 필드명 변경 대응
- **변경**: `recordCode` → `recordType`
- **수정 파일**:
  - `src/tracking/services/records.service.ts`
    - `createBeautyRecordImpl()` WHERE 조건 수정
    - `createRecordBase()` create 시 필드명 수정
    - Date 객체 변환 추가: `date: new Date(date)`
    - `createdAt: getNowKST()` 추가
  - `src/tracking/services/statistics.service.ts`
    - 모든 `recordCode:` → `recordType:` 일괄 변경

### 3. Beauty 통계 API 수정
- **파일**: `src/tracking/controllers/statistics.controller.ts`, `src/tracking/services/statistics.service.ts`
- **변경사항**:
  - `startDate`, `endDate` 쿼리 파라미터 추가
  - 날짜 범위 기반 통계 조회 가능
  - Date 객체 변환: `new Date(startDate)`, `new Date(endDate)`
  - 날짜 비교 로직 수정: `r.date.toISOString().split('T')[0] === date`

### 4. Beauty 통계 계산 로직 수정
- **파일**: `src/tracking/services/statistics.service.ts` - `analyzeBeautyDataForCorrectStructure()`
- **문제**: 잘못된 점수 환산 공식 (1~5점 기준으로 계산)
- **수정**:
  ```typescript
  // 기존 (잘못됨)
  const questionScore = Math.round((item.score / 5 * 100));

  // 수정 (올바름)
  dayInnerScore += item.score;  // 점수 그대로 합산
  ```

- **평균 계산 로직 수정**:
  ```typescript
  // 기존 (잘못됨)
  const innerScore = Math.round(totalInnerScore / 7);  // 무조건 7로 나눔

  // 수정 (올바름)
  const daysCount = weekDates.length;
  const innerScore = daysCount > 0 ? Math.round(totalInnerScore / daysCount) : 0;
  ```

- **최종 계산 공식**:
  - 이너뷰티 평균 = (전체 이너뷰티 합) / 검색일 수
  - 아우터뷰티 평균 = (전체 아우터뷰티 합) / 검색일 수
  - 종합 평균 = (이너뷰티 평균 + 아우터뷰티 평균) / 2
  - 일별 점수 = 해당일 4개 질문 점수 합

## 주요 이슈 및 해결

### 이슈 1: Prisma date 필드 타입 에러
- **문제**: `Invalid value for argument 'date': premature end of input. Expected ISO-8601 DateTime.`
- **원인**: schema.prisma에서 `date DateTime @db.Date`인데 문자열 직접 비교
- **해결**: `new Date(date)` 변환 추가

### 이슈 2: createdAt 필수 필드 누락
- **문제**: `Argument 'createdAt' is missing.`
- **원인**: schema에 `@default(now())` 없음
- **해결**: `createdAt: getNowKST()` 명시적 추가

### 이슈 3: 통계 데이터가 0으로 나옴
- **문제**: DB에서 가져온 date는 Date 객체인데 문자열과 직접 비교
- **해결**: `r.date.toISOString().split('T')[0] === date` 변환 후 비교

### 이슈 4: 점수가 비정상적으로 큼 (375점 등)
- **문제**: 25점을 5점 기준 환산 공식 적용 (25/5*100 = 500점)
- **해결**: 점수 그대로 합산, 환산 제거

### 이슈 5: 무조건 7일로 나눔
- **문제**: 검색 기간이 5일이든 7일이든 무조건 7로 나눔
- **해결**: `weekDates.length`로 실제 검색일 수만큼 나눔

## API 테스트 결과

### POST /api/tracking/records/beauty
```json
{
  "success": true,
  "statusCode": 201,
  "data": {
    "id": 109,
    "recordType": "BEAUTY",
    "date": "2025-11-03",
    "pointsEarned": 100
  }
}
```

### GET /api/tracking/statistics/beauty?startDate=2025-10-27&endDate=2025-11-02
```json
{
  "success": true,
  "data": {
    "summary": {
      "score": 61,
      "prevWeekDiff": 61,
      "weekScore": [...]
    },
    "detailData": {
      "innerBeauty": { "score": 61, ... },
      "outerBeauty": { "score": 61, ... }
    }
  }
}
```

### 5. Diet 기록 API 수정
- **파일**: `src/tracking/dto/records/records.dto.ts`, `src/tracking/services/records.service.ts`
- **변경사항**:
  - `CreateDietRecordDto`에서 `date` 파라미터 제거
  - 항상 한국 시간 오늘 날짜(`getKoreanToday()`)로 자동 저장
  - Swagger 예시 업데이트

### 6. Diet 기록 비즈니스 로직 구현
- **파일**: `src/tracking/services/records.service.ts` - `createDietRecordImpl()`
- **비즈니스 규칙**:
  - BREAKFAST: 1회/일 (100점)
  - LUNCH: 1회/일 (100점)
  - DINNER: 1회/일 (100점)
  - SNACK: 최대 3회/일 (0점)
  - MIDNIGHT_SNACK: 최대 3회/일 (0점)
- **검증 로직**:
  ```typescript
  // 같은 날짜의 모든 식단 기록 조회
  const todayRecords = await tx.userRecord.findMany({
    where: { userId, recordType: 'DIET', date: new Date(date) }
  });

  // 같은 식사 타입 카운트
  const sameDietTypeCount = todayRecords.filter(
    r => r.metadata?.diet === dietType
  ).length;

  // 제한 체크 및 ConflictException 발생
  if (['BREAKFAST','LUNCH','DINNER'].includes(dietType) && sameDietTypeCount >= 1) {
    throw new ConflictException(`오늘 이미 ${dietType} 기록을 완료했습니다`);
  }
  if (['SNACK','MIDNIGHT_SNACK'].includes(dietType) && sameDietTypeCount >= 3) {
    throw new ConflictException(`오늘 ${dietType} 기록은 최대 3회까지만 가능합니다`);
  }
  ```

### 7. Diet 통계 API 수정
- **파일**: `src/tracking/controllers/statistics.controller.ts`, `src/tracking/services/statistics.service.ts`
- **변경사항**:
  - `startDate`, `endDate` 쿼리 파라미터 추가
  - 날짜 범위 기반 통계 조회 가능
  - Date 객체 변환: `new Date(startDate)`, `new Date(endDate)`
  - 날짜 비교 로직 수정: Beauty와 동일한 패턴 적용

### 8. Diet 통계 점수 계산 로직 수정
- **파일**: `src/tracking/services/statistics.service.ts` - `getDietStatistics()`
- **문제**: 점수를 평균으로 계산 (합계 / 3)
- **수정**:
  ```typescript
  // 기존 (잘못됨)
  const summaryScore = Math.round((totalAllergyScore + totalHighFodmapCount + totalProcessedCount) / 3);

  // 수정 (올바름)
  const summaryScore = totalAllergyScore + totalHighFodmapCount + totalProcessedCount;
  ```
- **최종 계산 공식**:
  - summary.score = 알레르기점수 + 고포드맵점수 + 가공식품점수 (합계)

### 9. Diet 테스트 데이터 생성
- **파일**: `insert-diet-test-data.sql`
- **기간**: 2025-10-27 ~ 2025-11-02 (7일)
- **데이터 구성**:
  - 필수: BREAKFAST(1), LUNCH(1), DINNER(1) 매일
  - 선택: SNACK(1~3), MIDNIGHT_SNACK(1~3) 일별 변동
  - 총 49개 레코드
- **메타데이터**:
  - foodName: 현실적인 한식/양식 메뉴
  - allergyScore, highFodmapCount, processedCount 다양화
  - allergyFoods, highFodmapFoods, processedFoods 배열

## 주요 이슈 및 해결

### 이슈 1: Prisma date 필드 타입 에러
- **문제**: `Invalid value for argument 'date': premature end of input. Expected ISO-8601 DateTime.`
- **원인**: schema.prisma에서 `date DateTime @db.Date`인데 문자열 직접 비교
- **해결**: `new Date(date)` 변환 추가

### 이슈 2: createdAt 필수 필드 누락
- **문제**: `Argument 'createdAt' is missing.`
- **원인**: schema에 `@default(now())` 없음
- **해결**: `createdAt: getNowKST()` 명시적 추가

### 이슈 3: 통계 데이터가 0으로 나옴
- **문제**: DB에서 가져온 date는 Date 객체인데 문자열과 직접 비교
- **해결**: `r.date.toISOString().split('T')[0] === date` 변환 후 비교

### 이슈 4: 점수가 비정상적으로 큼 (375점 등)
- **문제**: 25점을 5점 기준 환산 공식 적용 (25/5*100 = 500점)
- **해결**: 점수 그대로 합산, 환산 제거

### 이슈 5: 무조건 7일로 나눔
- **문제**: 검색 기간이 5일이든 7일이든 무조건 7로 나눔
- **해결**: `weekDates.length`로 실제 검색일 수만큼 나눔

### 이슈 6: Diet 포인트가 첫 기록에만 지급
- **문제**: 첫 DIET 기록에만 100점 지급, 점심/저녁은 0점
- **원인**: metadata.diet 필드를 체크하지 않고 recordType만 체크
- **해결**: 같은 식사 타입 카운트로 변경, BREAKFAST/LUNCH/DINNER 각각 100점

### 이슈 7: Diet 중복 기록 미차단
- **문제**: 아침 2번, 간식 4번 등 중복 기록 가능
- **원인**: 중복 검증 로직 없음
- **해결**: ConflictException으로 제한 초과 시 차단

### 이슈 8: Method Signature Mismatch
- **문제**: `Expected 3 arguments, but got 1` at getStatisticsSummary
- **원인**: getDietStatistics() 시그니처 변경 후 호출부 미수정
- **해결**: `getDietStatistics(userId, startDate, endDate)` 파라미터 전달

## API 테스트 결과

### POST /api/tracking/records/beauty
```json
{
  "success": true,
  "statusCode": 201,
  "data": {
    "id": 109,
    "recordType": "BEAUTY",
    "date": "2025-11-03",
    "pointsEarned": 100
  }
}
```

### GET /api/tracking/statistics/beauty?startDate=2025-10-27&endDate=2025-11-02
```json
{
  "success": true,
  "data": {
    "summary": {
      "score": 61,
      "prevWeekDiff": 61,
      "weekScore": [...]
    },
    "detailData": {
      "innerBeauty": { "score": 61, ... },
      "outerBeauty": { "score": 61, ... }
    }
  }
}
```

### GET /api/tracking/statistics/diet?startDate=2025-10-27&endDate=2025-11-02
- summary.score: 합계 (알레르기 + 고포드맵 + 가공식품)
- 7일간 일별 섭취 현황 제공

## 다음 작업 예정
- Beauty/Diet 통계 멘트(comment) 룰베이스 구현
- 다른 기록 타입(SUPPLEMENT, FASTING, SLEEP, ACTIVITY) 통계 API 점검

## 특이사항
- 형님한테 많이 혼났음 (멀쩡한 코드 건드려서 롤백 난리)
- recordCode → recordType 변경으로 전체 파일 영향
- Beauty 점수 체계 변경 (1~5 → 25,20,15,10,5)으로 계산 로직 전면 수정
- Diet 비즈니스 규칙 엄격히 적용 (식사 타입별 제한, 포인트 차등 지급)
