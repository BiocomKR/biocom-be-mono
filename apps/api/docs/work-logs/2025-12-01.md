# 작업일지 2025-12-01

## 📋 작업 요약
- 영양제 통계 API에 currentCount, maxCount 필드 추가
- BullMQ 메시지큐 인스턴스 별도 구성 예정 (TODO)

---

## 1. 영양제 통계 API currentCount, maxCount 필드 추가

### 문제점
`GET /api/tracking/statistics/supplement` API에서 다른 통계 메소드들(beauty, diet, fasting, sleep, activity)에는 있는 `currentCount`, `maxCount` 필드가 누락됨

### 해결 방안
**SupplementStatisticsDto에 필드 추가 및 서비스 로직 수정**

**수정 파일:**
1. `src/tracking/dto/statistics/statistics.dto.ts`
2. `src/tracking/services/statistics.service.ts`

**수정 내용:**

#### 1) DTO 수정 (statistics.dto.ts:346-356)
```typescript
export class SupplementStatisticsDto {
  // ... 기존 필드들 ...

  @ApiProperty({
    description: '오늘 기록한 영양제 수',
    example: 2
  })
  currentCount: number;

  @ApiProperty({
    description: '하루 최대 기록 가능 수',
    example: 10
  })
  maxCount: number;
}
```

#### 2) Service 로직 수정 (statistics.service.ts)

**주요 변경사항:**
- `currentCount`: supplements 배열 길이 (통계 기간 내 기록이 있는 영양제 개수)
- `maxCount`: 10 (영양제 최대 등록 가능 개수)

**Line 639-646: 정상 응답**
```typescript
return {
  startDate,
  endDate,
  supplements,
  nutrients,
  currentCount: supplements.length,  // 배열 길이
  maxCount: 10,
};
```

**Line 507-514: 데이터 없는 경우**
```typescript
if (productIds.length === 0) {
  return {
    startDate,
    endDate,
    supplements: [],
    nutrients: [],
    currentCount: 0,
    maxCount: 10,
  };
}
```

**Line 1635-1642: NEWCOMER용 샘플 데이터**
```typescript
private getSampleSupplementStatistics(
  startDate: string,
  endDate: string,
): SupplementStatisticsDto {
  return {
    startDate,
    endDate,
    supplements: [],
    nutrients: [],
    currentCount: 0,
    maxCount: 10,
  };
}
```

### 테스트 결과
```bash
curl -X GET 'http://localhost:10804/api/tracking/statistics/supplement?startDate=2025-11-24&endDate=2025-11-30'
```

**응답:**
```json
{
  "success": true,
  "data": {
    "startDate": "2025-11-24",
    "endDate": "2025-11-30",
    "supplements": [
      // ... 4개 영양제 데이터
    ],
    "nutrients": [
      // ... 영양소 데이터
    ],
    "currentCount": 4,  ✅
    "maxCount": 10       ✅
  }
}
```

### 중요 개념 정리
- **currentCount**: supplements 배열 길이와 동일 (통계 기간 내 기록이 있는 영양제 개수)
- **maxCount**: 10 (영양제 루틴에 등록 가능한 최대 개수)

---

## 2. BullMQ Producer 구현 (GraphDB 동기화)

### 배경
- **목적**: biocom-api의 PostgreSQL 데이터를 Neo4j GraphDB에 동기화
- **아키텍처**: biocom-api (Producer) → Redis → biocom-mq (Consumer) → Neo4j
- **스펙 문서**: `docs/MQ_GUIDE.md` (biocom-mq 팀과 검증 완료)

### 구현 내용

#### A. 패키지 설치
```bash
npm install @nestjs/bullmq bullmq
```

#### B. 환경 설정
**파일**: `.env`
```bash
# Redis 설정 (BullMQ - GraphDB 동기화용)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
```

**파일**: `src/common/config/configuration.ts` (신규 생성)
```typescript
export default () => ({
  redis: {
    host: process.env.REDIS_HOST || 'localhost',
    port: parseInt(process.env.REDIS_PORT || '6379', 10),
    password: process.env.REDIS_PASSWORD || undefined,
  },
  // ...
});
```

#### C. Queue 타입 정의
**파일**: `src/graph-sync/types/queue.types.ts` (신규 생성)

**10개 Queue Job 타입:**
1. `UserSyncJob` - 사용자 기본 정보
2. `BeautySyncJob` - 뷰티 점수
3. `FoodSyncJob` - 음식 섭취 기록
4. `FastingSyncJob` - 단식 기록
5. `SleepSyncJob` - 수면 기록
6. `ActivitySyncJob` - 운동 기록
7. `AllergyReportSyncJob` - 알레르기 검사
8. `MissionSyncJob` - 미션 목록
9. `BalanceGameSyncJob` - 밸런스 게임
10. `SupplementSyncJob` - 영양제 섭취 기록 ✨

#### D. GraphSyncModule 생성
**파일**: `src/graph-sync/graph-sync.module.ts` (신규 생성)

```typescript
const QUEUE_NAMES = [
  'graph-sync-user',
  'graph-sync-beauty',
  'graph-sync-food',
  'graph-sync-fasting',
  'graph-sync-sleep',
  'graph-sync-activity',
  'graph-sync-allergy',
  'graph-sync-mission',
  'graph-sync-balance-game',
  'graph-sync-supplement', // ✨ 추가
];

@Module({
  imports: [
    ...QUEUE_NAMES.map((queueName) =>
      BullModule.registerQueue({
        name: queueName,
        connection: {
          host: process.env.REDIS_HOST || 'localhost',
          port: parseInt(process.env.REDIS_PORT || '6379', 10),
          password: process.env.REDIS_PASSWORD || undefined,
        },
      }),
    ),
  ],
  providers: [GraphSyncService],
  exports: [GraphSyncService],
})
export class GraphSyncModule {}
```

#### E. GraphSyncService 구현
**파일**: `src/graph-sync/graph-sync.service.ts` (신규 생성)

**10개 sync 메서드:**
- `syncUser(data: UserSyncJob): Promise<void>`
- `syncBeauty(data: BeautySyncJob): Promise<void>`
- `syncFood(data: FoodSyncJob): Promise<void>`
- `syncFasting(data: FastingSyncJob): Promise<void>`
- `syncSleep(data: SleepSyncJob): Promise<void>`
- `syncActivity(data: ActivitySyncJob): Promise<void>`
- `syncAllergyReport(data: AllergyReportSyncJob): Promise<void>`
- `syncMissions(data: MissionSyncJob): Promise<void>`
- `syncBalanceGames(data: BalanceGameSyncJob): Promise<void>`
- `syncSupplement(data: SupplementSyncJob): Promise<void>` ✨

**핵심 패턴: Fire-and-Forget**
```typescript
async syncBeauty(data: BeautySyncJob): Promise<void> {
  try {
    await this.beautyQueue.add('sync', data);  // Job 이름은 모두 'sync'로 통일
    this.logger.log(`✅ Beauty Queue 추가 완료: chartId=${data.chartId}`);
  } catch (error: any) {
    this.logger.error(`❌ Beauty Queue 추가 실패: error=${error.message}`);
  }
}
```

#### F. AppModule 등록
**파일**: `src/app.module.ts`

```typescript
import { BullModule } from '@nestjs/bullmq';
import { GraphSyncModule } from './graph-sync/graph-sync.module';

@Module({
  imports: [
    // BullMQ 전역 설정
    BullModule.forRoot({
      connection: {
        host: process.env.REDIS_HOST || 'localhost',
        port: parseInt(process.env.REDIS_PORT || '6379', 10),
        password: process.env.REDIS_PASSWORD || undefined,
      },
    }),

    // GraphSync 모듈
    GraphSyncModule,

    // ...
  ],
})
export class AppModule {}
```

#### G. RecordsModule 연동
**파일**: `src/tracking/records.module.ts`

```typescript
import { GraphSyncModule } from '../graph-sync/graph-sync.module';

@Module({
  imports: [
    PointModule,
    HttpModule,
    GraphSyncModule,  // GraphDB 동기화를 위해 import
  ],
  // ...
})
export class RecordsModule {}
```

**파일**: `src/tracking/services/records.service.ts`

```typescript
import { GraphSyncService } from '../../graph-sync/graph-sync.service';

@Injectable()
export class RecordsService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly pointService: PointService,
    private readonly httpService: HttpService,
    private readonly graphSyncService: GraphSyncService,  // 의존성 주입
  ) {}

  // TODO: 각 CREATE 메서드에서 GraphSyncService 호출 추가
}
```

### 구현 완료 항목
- ✅ BullMQ 패키지 설치
- ✅ Redis 환경 설정
- ✅ Queue 타입 정의 (10개) - SupplementSyncJob 추가 ✨
- ✅ GraphSyncModule 생성 (10개 Queue 등록)
- ✅ GraphSyncService 구현 (10개 sync 메서드) - syncSupplement 추가 ✨
- ✅ AppModule에 BullMQ 전역 등록
- ✅ RecordsModule에 GraphSyncModule import
- ✅ RecordsService에 GraphSyncService 의존성 주입
- ✅ 빌드 테스트 성공

### 차후 작업 (다음 세션)
- [ ] 각 API 엔드포인트에서 GraphSyncService 호출 추가
  - createBeautyRecord → syncBeauty
  - createDietRecord → syncFood
  - createSupplementRecord → syncSupplement ✨ (Queue 추가됨!)
  - createFastingRecord → syncFasting
  - createSleepRecord → syncSleep
  - createActivityRecord → syncActivity
- [ ] User API에 syncUser 호출 추가
- [ ] 단위 테스트 작성
- [ ] E2E 테스트 (biocom-mq와 통합)

### 중요 스펙 사항
**Queue 이름 규칙 (MQ_GUIDE.md 준수):**
- Queue 이름: `graph-sync-*` 형식
- Job 이름: 모두 `'sync'`로 통일

**Fire-and-Forget 패턴:**
- Queue 실패해도 PostgreSQL 저장은 성공
- 트랜잭션 외부에서 Queue 호출
- 에러는 Logger로 기록만 하고 throw하지 않음

---

## 3. 영양제 배치 중복 실행 문제 해결

### 문제 발견
배치 로직 점검 중 심각한 문제 발견:
1. **중복 데이터 생성**: user_records 테이블에 동일한 데이터가 3배씩 생성됨
2. **record_type 오타**: `SUPPLEMENTS` (복수형) → `SUPPLEMENT` (단수형) 오류
3. **멀티 파드 환경 문제**: GKE 2개 파드 + 로컬 1개 = 총 3번 중복 실행

### 데이터 분석 결과

#### 중복 생성 패턴
| 날짜 | 예상 건수 | 실제 건수 | 중복 배수 |
|------|----------|----------|----------|
| 2025-11-30 | 3 | 6 | **2배** |
| 2025-12-01 | 3 | 18 | **6배** |
| 2025-12-02 | 3 | 15 | **5배** |
| 2025-12-03 | 3 | 15 | **5배** |
| 2025-12-04 | 3 | 15 | **5배** |
| 2025-12-05 | 3 | 15 | **5배** |
| 2025-12-06 | 3 | 15 | **5배** |
| 2025-12-07 | 3 | 12 | **4배** |

#### 생성 시점 분석
- `2025-12-01 01:00:00` (84건): CronJob 자동 실행 (월요일 00:10 UTC)
- `2025-12-01 10:02:58` (42건): 수동 실행 2회

### 근본 원인
1. **NestJS @Cron 데코레이터**가 각 파드에서 실행됨 (2개 파드 = 2번 실행)
2. **Kubernetes CronJob**도 동시에 실행됨 (1번 실행)
3. **로컬 개발 환경**에서도 실행됨 (1번 실행)
4. **총 3-4번 중복 실행**

### 해결 방안

#### A. NestJS @Cron 데코레이터 비활성화
**파일**: `src/challenge/challenge-scheduler.service.ts`

**3개 배치 모두 비활성화:**
```typescript
// @Cron('0 5 * * 1', {
//   name: 'activate-challenges',
//   timeZone: 'Asia/Seoul',
// })
async handleChallengeActivation() {
  // NOTE: Kubernetes CronJob으로 실행됨
}

// @Cron('0 10 * * 1', {
//   name: 'create-weekly-supplements',
//   timeZone: 'Asia/Seoul',
// })
async handleWeeklySupplementCreation() {
  // NOTE: Kubernetes CronJob으로 실행됨
}

// @Cron('0 0 * * *', {
//   name: 'expire-challenges',
//   timeZone: 'Asia/Seoul',
// })
async handleChallengeExpiration() {
  // NOTE: Kubernetes CronJob으로 실행됨
}
```

#### B. 중복 방지 로직 추가
**파일**: `src/challenge/challenge-scheduler.service.ts:275-278`

```typescript
await tx.userRecord.createMany({
  data: recordsToCreate,
  skipDuplicates: true,  // 👈 중복 시 자동 스킵
});
```

#### C. DB UNIQUE 제약 추가
**파일**: `prisma/migrations/add_user_records_supplement_unique_index.sql`

```sql
CREATE UNIQUE INDEX IF NOT EXISTS user_records_supplement_unique
ON user_records (
  user_id,
  record_type,
  date,
  ((metadata->>'productId')::int)
)
WHERE record_type = 'SUPPLEMENT';
```

**효과:**
- 동일한 사용자, 날짜, 영양제에 대해 하나의 레코드만 존재 보장
- `skipDuplicates: true`와 함께 사용하여 안전한 배치 실행
- 서버 멈춤 없이 중복 데이터 자동 방지

#### D. record_type 오타 수정

**문제:**
- 배치 코드에서 `'SUPPLEMENTS'` (복수형)로 잘못 생성
- 올바른 값은 `'SUPPLEMENT'` (단수형)

**수정 SQL**: `prisma/migrations/fix_supplement_record_type.sql`
```sql
UPDATE user_records
SET record_type = 'SUPPLEMENT'
WHERE record_type = 'SUPPLEMENTS';
-- 결과: 21건 수정
```

#### E. RecordType Enum 생성
**파일**: `src/common/enums/record-type.enum.ts` (신규 생성)

**문제:**
- 하드코딩된 문자열 사용 (`'SUPPLEMENT'`, `'DIET'` 등)
- 타입 안전성 부족
- 오타 가능성

**해결:**
```typescript
export enum RecordType {
  BEAUTY = 'BEAUTY',
  DIET = 'DIET',
  SUPPLEMENT = 'SUPPLEMENT',
  FASTING = 'FASTING',
  SLEEP = 'SLEEP',
  ACTIVITY = 'ACTIVITY',
  QUIZ = 'QUIZ',
  DAILY_MISSION = 'DAILY_MISSION',
  DECLARATION = 'DECLARATION',
}

export const RecordTypeLabel: Record<RecordType, string> = {
  [RecordType.BEAUTY]: '뷰티',
  [RecordType.DIET]: '식단',
  [RecordType.SUPPLEMENT]: '영양제',
  // ...
};

export const MainRecordTypes = [
  RecordType.BEAUTY,
  RecordType.DIET,
  RecordType.SUPPLEMENT,
  RecordType.FASTING,
  RecordType.SLEEP,
  RecordType.ACTIVITY,
] as const;
```

**적용:**
```typescript
// Before
recordType: 'SUPPLEMENT',

// After
recordType: RecordType.SUPPLEMENT,
```

### 배포 결과

#### 수정된 파일
1. `src/challenge/challenge-scheduler.service.ts` - @Cron 비활성화, enum 적용, skipDuplicates 추가
2. `src/common/enums/record-type.enum.ts` - RecordType enum 신규 생성
3. `src/common/enums/index.ts` - RecordType export 추가
4. `prisma/migrations/add_user_records_supplement_unique_index.sql` - UNIQUE 인덱스
5. `prisma/migrations/fix_supplement_record_type.sql` - record_type 수정

#### 배포 정보
- **이미지**: `asia-northeast3-docker.pkg.dev/api-dev-biocom/biocom-api/biocom-api:20251201123813`
- **GKE 파드**: 2개 재시작 완료
- **배포 시간**: 2025-12-01 12:38 KST

#### DB 마이그레이션
```sql
-- 1. record_type 수정
UPDATE user_records SET record_type = 'SUPPLEMENT' WHERE record_type = 'SUPPLEMENTS';
-- 결과: 21건 수정

-- 2. UNIQUE 인덱스 생성
CREATE UNIQUE INDEX user_records_supplement_unique ON user_records (...);
-- 결과: 성공
```

### 검증 결과

#### 1. GKE 로그 확인
- ✅ NestJS 파드에서 스케줄러 실행 로그 없음 (정상)
- ✅ Kubernetes CronJob만 실행됨

#### 2. CronJob 상태
```bash
NAME                        SCHEDULE      LAST SCHEDULE
activate-challenges         5 15 * * 0    12h (정상)
create-weekly-supplements   10 15 * * 0   12h (정상)
expire-challenges           0 15 * * *    12h (정상)
```

#### 3. 다음 실행 예정
- **영양제 생성**: 다음 주 월요일 00:10 (KST) = 12월 8일 일요일 15:10 (UTC)
- **챌린지 활성화**: 다음 주 월요일 00:05 (KST)
- **챌린지 만료**: 매일 자정 00:00 (KST)

### 기대 효과
1. ✅ **중복 실행 완전 차단**: Kubernetes CronJob만 실행 (1번)
2. ✅ **중복 데이터 방지**: UNIQUE 제약 + skipDuplicates
3. ✅ **타입 안전성 확보**: RecordType enum 사용
4. ✅ **오타 방지**: `SUPPLEMENTS` → `SUPPLEMENT` 수정
5. ✅ **서버 안정성**: 런타임 에러 발생 시에도 서버 정상 동작

### 향후 개선 사항
- [ ] 다른 하드코딩된 문자열도 enum으로 교체 (점진적 적용)
- [ ] 배치 실행 모니터링 대시보드 구축
- [ ] 중복 데이터 자동 정리 스크립트 작성

---

## 4. MQ_GUIDE.md 업데이트 반영 (SupplementSyncJob 추가)

### 배경
- **발견**: MQ_GUIDE.md가 업데이트되어 10번째 Queue가 추가됨
- **기존**: 9개 Queue (User, Beauty, Food, Fasting, Sleep, Activity, Allergy, Mission, BalanceGame)
- **추가**: `graph-sync-supplement` Queue (영양제 섭취 기록)

### 추가된 타입 스펙
```typescript
// 10. Supplement Queue - 영양제 섭취 기록
export interface SupplementSyncJob {
  chartId: string;
  dateId: string;
  date: string;
  supplements: Array<{
    supplementId: string;
    supplementName: string;
    intakeCount: number;
    recommendedCount: number;
    nutrients: string[];
  }>;
}
```

### 수정 내역

#### A. queue.types.ts 업데이트
**파일**: `src/graph-sync/types/queue.types.ts`

```typescript
/**
 * BullMQ Queue Job 타입 정의
 * - 총 10개 Queue Job 타입 정의 (9개 → 10개)
 */

// ... 기존 9개 타입 ...

/**
 * 10. Supplement Queue - 영양제 섭취 기록
 */
export interface SupplementSyncJob {
  chartId: string;
  dateId: string;
  date: string;
  supplements: Array<{
    supplementId: string;
    supplementName: string;
    intakeCount: number;
    recommendedCount: number;
    nutrients: string[];
  }>;
}
```

#### B. graph-sync.module.ts 업데이트
**파일**: `src/graph-sync/graph-sync.module.ts`

```typescript
/**
 * Queue 이름 목록 (10개)
 */
const QUEUE_NAMES = [
  'graph-sync-user',
  'graph-sync-beauty',
  'graph-sync-food',
  'graph-sync-fasting',
  'graph-sync-sleep',
  'graph-sync-activity',
  'graph-sync-allergy',
  'graph-sync-mission',
  'graph-sync-balance-game',
  'graph-sync-supplement', // ✨ 추가
];
```

#### C. graph-sync.service.ts 업데이트
**파일**: `src/graph-sync/graph-sync.service.ts`

**Constructor 수정:**
```typescript
constructor(
  // ... 기존 9개 Queue ...
  @InjectQueue('graph-sync-supplement') private supplementQueue: Queue,
) {}
```

**syncSupplement 메서드 추가:**
```typescript
/**
 * 10. 영양제 섭취 기록 동기화
 */
async syncSupplement(data: SupplementSyncJob): Promise<void> {
  try {
    await this.supplementQueue.add('sync', data);
    this.logger.log(
      `✅ Supplement Queue 추가 완료: chartId=${data.chartId}, date=${data.date}, supplements=${data.supplements.length}개`,
    );
  } catch (error: any) {
    this.logger.error(
      `❌ Supplement Queue 추가 실패: chartId=${data.chartId}, error=${error.message}`,
    );
  }
}
```

### 변경 사항 요약
- ✅ SupplementSyncJob 타입 추가
- ✅ graph-sync-supplement Queue 등록
- ✅ supplementQueue 의존성 주입
- ✅ syncSupplement 메서드 구현
- ✅ 빌드 테스트 성공

### 영향 범위
- **Queue 개수**: 9개 → 10개
- **MQ_GUIDE.md 준수**: 완전 준수
- **하위 호환성**: 기존 9개 Queue에 영향 없음

---

## 📝 작업 시간
- 영양제 통계 API 개선: 1시간
- BullMQ Producer 구현: 2시간
  - MQ_GUIDE.md 분석: 30분
  - Queue 타입 정의: 30분
  - GraphSyncModule/Service 구현: 1시간
- BullMQ Supplement Queue 추가: 30분
  - MQ_GUIDE.md 재분석: 10분
  - 타입 및 Queue 추가: 15분
  - 빌드 테스트: 5분
- 배치 중복 실행 문제 해결: 3시간
  - 문제 분석: 30분
  - 코드 수정: 1시간
  - DB 마이그레이션: 30분
  - 배포 및 검증: 1시간

## 🎯 주요 성과
1. ✅ 영양제 통계 API 완성도 향상 (currentCount, maxCount 추가)
2. ✅ BullMQ Producer 구현 완료 (GraphDB 동기화 준비, 10개 Queue)
3. ✅ MQ_GUIDE.md 스펙 완전 준수 (Supplement Queue 추가)
4. ✅ 배치 중복 실행 문제 완전 해결
5. ✅ RecordType enum 도입으로 타입 안전성 확보
6. ✅ DB 레벨 중복 방지 시스템 구축
