# 작업일지 2025-11-27

## 📋 작업 요약
- 영양제 기록 조회 API 정렬 순서 수정 (display_order 기준)
- 영양제 기록 maxCount 수정 (10으로 변경)
- 영양제 통계 API 구현 (매트릭스 구조)

---

## 1. 영양제 기록 조회 API 정렬 순서 수정

### 문제점
`GET /api/tracking/records` 에서 SUPPLEMENT 타입 응답 시, user_records의 저장 순서대로 반환되어 메타드림(2그룹)이 바이오 밸런스(1그룹)보다 먼저 나오는 문제

### 해결 방안
**user_supplement_routine의 display_order로 정렬**

**파일:** `src/tracking/services/records.service.ts`

**수정 내용:**
```typescript
// transformSupplementRecords() 함수에 userId 파라미터 추가
private async transformSupplementRecords(
  userId: number,
  typeRecords: any[],
)

// user_supplement_routine에서 display_order 조회
const routineList = await this.prisma.userSupplementRoutine.findMany({
  where: {
    userId,
    isActive: true,
    productId: { in: productIds },
  },
  select: {
    productId: true,
    displayOrder: true,
  },
});

// display_order로 정렬
supplementList.sort((a, b) => a.displayOrder - b.displayOrder);
```

**테스트 결과:**
```json
{
  "metadata": [
    { "productId": 4, "productName": "바이오 밸런스" },  // displayOrder: 1
    { "productId": 5, "productName": "클린 밸런스" },    // displayOrder: 2
    { "productId": 1, "productName": "당당케어" },      // displayOrder: 3
    { "productId": 41, "productName": "메타드림" }      // displayOrder: 4
  ]
}
```

---

## 2. 영양제 기록 maxCount 수정

### 문제점
`getRecordLimit()` 함수에서 SUPPLEMENT 타입의 maxCount가 -1(무제한)로 설정되어 있음

### 해결 방안
**maxCount를 10으로 변경**

**파일:** `src/tracking/services/records.service.ts` (line 930-932)

```typescript
// Before
case 'SUPPLEMENT':
  // 무제한
  return { currentCount: records.length, maxCount: -1 };

// After
case 'SUPPLEMENT':
  // 1일 10회
  return { currentCount: records.length, maxCount: 10 };
```

---

## 3. 영양제 통계 API 구현 (매트릭스 구조)

### 요구사항
영양제 섭취 이력을 7일간(월~일) 매트릭스 형태로 표현:
- **파란색 박스**: 루틴에 있고 섭취함 (intakeCount > 0)
- **회색 박스**: 루틴에 있지만 섭취 안함 (intakeCount = 0)
- **빈칸**: 루틴에서 삭제되어 데이터 없음 (null)

**비즈니스 규칙:**
1. 1그룹(isDefault=true) 영양제는 7일 전체 데이터 존재 (null 없음)
2. 2,3그룹 영양제는 루틴 추가/삭제에 따라 null 처리
3. 정렬 순서: 현재(오늘 기준) user_supplement_routine의 display_order

### API 엔드포인트
```
GET /api/tracking/statistics/supplement?startDate=2025-11-25&endDate=2025-12-01
```

### 구현 내용

#### 1) DTO 생성
**파일:** `src/tracking/dto/statistics/statistics.dto.ts`

```typescript
// 일별 영양제 섭취 상태 DTO
export class DailySupplementStatusDto {
  date: string;
  intakeCount: number | null;  // null = 해당 날짜 루틴에 없음
  recommendedCount: number | null;
}

// 영양제별 주간 통계 DTO
export class SupplementWeeklyStatsDto {
  productId: number;
  productName: string;
  displayOrder: number;  // 999 = 루틴에서 삭제됨
  dailyStats: DailySupplementStatusDto[];
}

// 영양제 통계 응답 DTO (매트릭스 구조)
export class SupplementStatisticsDto {
  startDate: string;
  endDate: string;
  supplements: SupplementWeeklyStatsDto[];
}
```

#### 2) Service 로직
**파일:** `src/tracking/services/statistics.service.ts`

**주요 로직:**
```typescript
async getSupplementStatistics(
  userId: number,
  startDate: string,
  endDate: string,
  isNewcomer: boolean = false,
): Promise<SupplementStatisticsDto> {
  // 1. 주간 7일 날짜 배열 생성 (KST 타임존 처리)
  const weekDates: string[] = [];
  const currentDate = new Date(startDate + 'T00:00:00+09:00');
  for (let i = 0; i < 7; i++) {
    weekDates.push(currentDate.toISOString().split('T')[0]);
    currentDate.setDate(currentDate.getDate() + 1);
  }

  // 2. 해당 주간의 모든 영양제 기록 조회
  const supplementRecords = await this.prisma.userRecord.findMany({
    where: {
      userId,
      recordType: 'SUPPLEMENT',
      date: {
        gte: new Date(startDate + 'T00:00:00+09:00'),
        lte: new Date(endDate + 'T23:59:59+09:00'),
      },
    },
    orderBy: { date: 'asc' },
  });

  // 3. 주간에 등장한 모든 productId 추출
  const productIdSet = new Set<number>();
  supplementRecords.forEach((record) => {
    const metadata = record.metadata as any;
    if (metadata?.productId) {
      productIdSet.add(metadata.productId);
    }
  });

  // 4. 현재(오늘 기준) user_supplement_routine에서 display_order 조회
  const routineList = await this.prisma.userSupplementRoutine.findMany({
    where: {
      userId,
      isActive: true,
      productId: { in: productIds },
    },
    select: {
      productId: true,
      displayOrder: true,
    },
  });

  // 5. Product 정보 조회 (name, frequency_per_day)
  const products = await this.prisma.product.findMany({
    where: { id: { in: productIds } },
    select: { id: true, name: true, productInfo: true },
  });

  // 6. 날짜별 + productId별로 그룹화하여 intakeCount 계산
  const recordsByDateAndProduct = new Map<string, Map<number, any>>();
  supplementRecords.forEach((record) => {
    const dateStr = record.date.toISOString().split('T')[0];
    const metadata = record.metadata as any;
    const intakeCount = [
      metadata.morning || false,
      metadata.afternoon || false,
      metadata.evening || false,
    ].filter(Boolean).length;

    recordsByDateAndProduct.get(dateStr)!.set(productId, {
      intakeCount,
      recommendedCount: frequencyPerDay,
    });
  });

  // 7. 영양제별 주간 통계 생성 (7일 매트릭스)
  const supplements = productIds.map((productId) => {
    const dailyStats = weekDates.map((dateStr) => {
      const record = recordsByDateAndProduct.get(dateStr)?.get(productId);

      if (record) {
        return {
          date: dateStr,
          intakeCount: Math.min(record.intakeCount, record.recommendedCount),
          recommendedCount: record.recommendedCount,
        };
      } else {
        // 데이터 없음 = null
        return {
          date: dateStr,
          intakeCount: null,
          recommendedCount: null,
        };
      }
    });

    return {
      productId,
      productName: productInfo.name,
      displayOrder: displayOrderMap.get(productId) || 999,
      dailyStats,
    };
  });

  // 8. display_order 순서로 정렬
  supplements.sort((a, b) => a.displayOrder - b.displayOrder);

  return { startDate, endDate, supplements };
}
```

#### 3) Controller 엔드포인트
**파일:** `src/tracking/controllers/statistics.controller.ts`

```typescript
@Get('supplement')
@ApiOperation({
  summary: '영양제 상세 통계 조회',
  description: '영양제 섭취 이력을 매트릭스 형태로 조회합니다 (7일간, 월~일)',
})
@ApiQuery({
  name: 'startDate',
  required: true,
  description: '시작일 (YYYY-MM-DD, 월요일)',
  example: '2025-07-20',
})
@ApiQuery({
  name: 'endDate',
  required: true,
  description: '종료일 (YYYY-MM-DD, 일요일)',
  example: '2025-07-26',
})
async getSupplementStatistics(
  @Request() req: any,
  @Query('startDate') startDate: string,
  @Query('endDate') endDate: string,
) {
  const data = await this.statisticsService.getSupplementStatistics(
    req.user.id,
    startDate,
    endDate,
    req.isNewcomer || false,
  );
  return {
    success: true,
    data,
    isReal: req.isNewcomer ? false : true,
    message: '영양제 통계 조회 성공',
  };
}
```

### 테스트 결과
```json
{
  "success": true,
  "data": {
    "startDate": "2025-11-25",
    "endDate": "2025-12-01",
    "supplements": [
      {
        "productId": 4,
        "productName": "바이오 밸런스",
        "displayOrder": 1,
        "dailyStats": [
          { "date": "2025-11-24", "intakeCount": 0, "recommendedCount": 1 },
          { "date": "2025-11-25", "intakeCount": 0, "recommendedCount": 1 },
          { "date": "2025-11-26", "intakeCount": 0, "recommendedCount": 1 },
          { "date": "2025-11-27", "intakeCount": 0, "recommendedCount": 1 },
          { "date": "2025-11-28", "intakeCount": 0, "recommendedCount": 1 },
          { "date": "2025-11-29", "intakeCount": 0, "recommendedCount": 1 },
          { "date": "2025-11-30", "intakeCount": 0, "recommendedCount": 1 }
        ]
      },
      {
        "productId": 5,
        "productName": "클린 밸런스",
        "displayOrder": 2,
        "dailyStats": [
          { "date": "2025-11-26", "intakeCount": 1, "recommendedCount": 2 }
          // ... (11/26일에 1/2 섭취)
        ]
      },
      {
        "productId": 41,
        "productName": "메타드림",
        "displayOrder": 999,  // 루틴에서 삭제됨
        "dailyStats": [
          { "date": "2025-11-24", "intakeCount": null, "recommendedCount": null },  // 빈칸
          { "date": "2025-11-25", "intakeCount": null, "recommendedCount": null },  // 빈칸
          { "date": "2025-11-26", "intakeCount": null, "recommendedCount": null },  // 빈칸
          { "date": "2025-11-27", "intakeCount": 1, "recommendedCount": 1 },      // 파란색
          { "date": "2025-11-28", "intakeCount": null, "recommendedCount": null }   // 빈칸
        ]
      }
    ]
  },
  "isReal": true,
  "message": "영양제 통계 조회 성공"
}
```

### UI 렌더링 규칙
- **intakeCount = null, recommendedCount = null** → 빈칸
- **intakeCount = 0** → 회색 박스 (0%)
- **intakeCount > 0** → 파란색 박스 (intakeCount / recommendedCount 비율)
  - 1/3 → 33% 파란색
  - 1/2 → 50% 파란색
  - 2/2 → 100% 파란색

---

## 4. 영양소 섭취 통계 구현

### 요구사항
영양제 통계 API에 영양소별 섭취량 통계 추가:
- 1그룹(isDefault=true) 영양제만 대상
- 영양소별로 주간 섭취량/권장량 계산
- **중요**: 실제 먹은 횟수 기준으로 계산 (morning, afternoon, evening 각각 카운트)
- 하루 권장량 초과 불가 (Math.min 처리)
- 가나다 순 정렬

### 계산 로직

**권장 섭취량 (분모):**
```
recommendedCount = Σ(frequency_per_day) × 7

예) 아연이 포함된 영양제:
- 바이오밸런스: frequency_per_day = 1
- 클린밸런스: frequency_per_day = 2
→ (1 + 2) × 7 = 21
```

**실제 섭취량 (분자):**
```
intakeCount = Σ(실제 먹은 횟수, 단 하루 권장량 초과 불가)

예) 2025-11-24 ~ 2025-11-30 기간 아연 섭취:
- 바이오밸런스 (freq=1):
  - 11/25: evening(1) → min(1, 1) = 1
  - 11/26: evening(1) → min(1, 1) = 1
  - 11/27: evening(1) → min(1, 1) = 1
  - 합계: 3

- 클린밸런스 (freq=2):
  - 11/25: morning(1) + afternoon(1) → min(2, 2) = 2
  - 11/26: morning(1) + afternoon(1) → min(2, 2) = 2
  - 11/27: morning(1) → min(1, 2) = 1
  - 합계: 5

→ 총 섭취량: 3 + 5 = 8
→ 결과: 8 / 21
```

**중요 제약:**
- 하루 3번(morning, afternoon, evening) 먹어도 권장량까지만 카운트
- 예: frequency_per_day=2인데 3번 먹음 → 2로 카운트

### 구현 내용

#### 1) DTO 수정
**파일:** `src/tracking/dto/statistics/statistics.dto.ts`

```typescript
// 영양소 섭취량 DTO (percentage 제거)
export class NutrientIntakeDto {
  @ApiProperty({ description: '영양소명' })
  nutrientName: string;

  @ApiProperty({ description: '실제 섭취량 (횟수)' })
  intakeCount: number;

  @ApiProperty({ description: '권장 섭취량 (횟수)' })
  recommendedCount: number;
}

// SupplementStatisticsDto에 nutrients 필드 추가
export class SupplementStatisticsDto {
  startDate: string;
  endDate: string;
  supplements: SupplementWeeklyStatsDto[];
  nutrients: NutrientIntakeDto[];  // 추가
}
```

#### 2) Service 로직 구현
**파일:** `src/tracking/services/statistics.service.ts`

**calculateNutrientIntake() 함수 구현:**
```typescript
private async calculateNutrientIntake(
  userId: number,
  startDate: string,
  endDate: string,
  supplementRecords: any[],
) {
  // 1. 1그룹 영양제 조회 (isDefault: true)
  const defaultRoutine = await this.prisma.userSupplementRoutine.findMany({
    where: { userId, isDefault: true, isActive: true },
    select: { productId: true },
  });

  // 2. 1그룹 영양제들의 영양소 조회
  const supplementNutrients = await this.prisma.supplementNutrient.findMany({
    where: { productId: { in: defaultProductIds } },
    select: { productId: true, nutrientName: true },
  });

  // 3. 각 영양제의 frequency_per_day 조회
  const products = await this.prisma.product.findMany({
    where: { id: { in: defaultProductIds } },
    select: { id: true, productInfo: true },
  });

  const productFrequencyMap = new Map<number, number>(
    products.map((p) => [
      p.id,
      (p.productInfo as any)['frequency_per_day'] || 1,
    ]),
  );

  // 4. 영양소별로 그룹화 (영양소 → 포함 영양제 목록)
  const nutrientToProducts = new Map<string, Set<number>>();
  supplementNutrients.forEach((sn) => {
    if (!nutrientToProducts.has(sn.nutrientName)) {
      nutrientToProducts.set(sn.nutrientName, new Set());
    }
    nutrientToProducts.get(sn.nutrientName)!.add(sn.productId);
  });

  // 5. 영양제별 실제 섭취 횟수 계산
  const productIntakeCounts = new Map<number, number>();
  defaultProductIds.forEach((productId) => {
    productIntakeCounts.set(productId, 0);
  });

  supplementRecords.forEach((record) => {
    const metadata = record.metadata as any;
    const productId = metadata?.productId;

    if (!defaultProductIds.includes(productId)) {
      return; // 1그룹 아니면 스킵
    }

    const morning = metadata.morning || false;
    const afternoon = metadata.afternoon || false;
    const evening = metadata.evening || false;

    // 실제로 몇 번 먹었는지 카운트
    let dailyIntakeCount = 0;
    if (morning) dailyIntakeCount++;
    if (afternoon) dailyIntakeCount++;
    if (evening) dailyIntakeCount++;

    if (dailyIntakeCount > 0) {
      // 🔥 중요: 하루 권장량을 초과할 수 없음
      const frequency = productFrequencyMap.get(productId) || 1;
      const validIntakeCount = Math.min(dailyIntakeCount, frequency);

      const currentCount = productIntakeCounts.get(productId) || 0;
      productIntakeCounts.set(productId, currentCount + validIntakeCount);
    }
  });

  // 6. 영양소별 섭취량 계산
  const nutrients = Array.from(nutrientToProducts.entries()).map(
    ([nutrientName, productIds]) => {
      // 권장 섭취량 (분모) = Σ(frequency_per_day) × 7
      let recommendedCount = 0;
      productIds.forEach((productId) => {
        const frequency = productFrequencyMap.get(productId) || 1;
        recommendedCount += frequency * 7;
      });

      // 실제 섭취량 (분자) = Σ(실제 먹은 횟수)
      let intakeCount = 0;
      productIds.forEach((productId) => {
        const actualIntakeCount = productIntakeCounts.get(productId) || 0;
        intakeCount += actualIntakeCount;
      });

      return {
        nutrientName,
        intakeCount,
        recommendedCount,
      };
    },
  );

  // 7. 영양소명 가나다순으로 정렬
  nutrients.sort((a, b) => a.nutrientName.localeCompare(b.nutrientName, 'ko'));

  return nutrients;
}
```

**getSupplementStatistics()에서 호출:**
```typescript
// 9. 영양소 섭취 통계 계산
const nutrients = await this.calculateNutrientIntake(
  userId,
  startDate,
  endDate,
  supplementRecords,
);

return { startDate, endDate, supplements, nutrients };
```

### 테스트 결과
```json
{
  "success": true,
  "data": {
    "startDate": "2025-11-24",
    "endDate": "2025-11-30",
    "supplements": [ /* ... */ ],
    "nutrients": [
      {
        "nutrientName": "가르시니아캄보지아추출물물",
        "intakeCount": 6,
        "recommendedCount": 14
      },
      {
        "nutrientName": "마그네슘",
        "intakeCount": 3,
        "recommendedCount": 7
      },
      {
        "nutrientName": "아연",
        "intakeCount": 8,
        "recommendedCount": 21
      },
      {
        "nutrientName": "클로렐라",
        "intakeCount": 5,
        "recommendedCount": 14
      }
    ]
  }
}
```

### 검증
**아연 섭취량 계산 확인:**
- 바이오밸런스 (freq=1): 11/25, 26, 27 각 1회 = 3
- 클린밸런스 (freq=2): 11/25(2), 11/26(2), 11/27(1) = 5
- **총 8회 / 권장 21회** ✅

---

## 5. KST 타임존 문제 수정

### 문제점
모든 통계 API에서 날짜 처리 시 `toISOString().split('T')[0]` 사용으로 인해 UTC 기준으로 변환되어 날짜가 하루 전으로 나타남

**예시:**
- 요청: `startDate=2025-11-24`
- 응답: `dailyStats[0].date = "2025-11-23"` ❌

### 해결 방안
**formatKoreanDate() 유틸 함수 사용**

**파일:** `src/common/utils/korea-date.util.ts`

```typescript
/**
 * Date 객체를 한국 시간 기준 YYYY-MM-DD 형식으로 변환
 */
export function formatKoreanDate(date: Date): string {
  // 한국 시간으로 변환 후 날짜 부분만 추출
  const koreanTime = new Date(date.getTime() + (9 * 60 * 60 * 1000));
  return koreanTime.toISOString().split('T')[0];
}
```

### 수정 내용
**파일:** `src/tracking/services/statistics.service.ts`

1. **import 추가:**
```typescript
import { getDayOfWeek, formatKoreanDate } from '../../common/utils/korea-date.util';
```

2. **모든 toISOString().split('T')[0] 교체 (총 14곳):**
```typescript
// Before
const dateStr = date.toISOString().split('T')[0];

// After
const dateStr = formatKoreanDate(date);
```

3. **날짜 배열 생성 로직 수정:**
```typescript
// Before (UTC 기준)
const weekDates: string[] = [];
const current = new Date(startDate);
for (let i = 0; i < 7; i++) {
  dates.push(current.toISOString().split('T')[0]);
  current.setDate(current.getDate() + 1);
}

// After (KST 기준)
const weekDates: string[] = [];
const [year, month, day] = startDate.split('-').map(Number);
const current = new Date(year, month - 1, day);
for (let i = 0; i < 7; i++) {
  dates.push(formatKoreanDate(current));
  current.setDate(current.getDate() + 1);
}
```

### 영향 받은 함수들
- `getWeekDateRange()` - 주간 날짜 범위 계산
- `getPreviousWeekDateRange()` - 이전 주 날짜 범위
- `generateWeekDates()` - 7일 날짜 배열 생성
- `getBeautyStatistics()` - 뷰티 통계
- `getDietStatistics()` - 식단 통계
- `getSupplementStatistics()` - 영양제 통계
- `getFastingStatistics()` - 간헐적단식 통계
- `getSleepStatistics()` - 수면 통계
- `getActivityStatistics()` - 활동 통계
- `getPreviousDate()` - 이전 날짜 계산
- `getTodayRecordLimit()` - 오늘 기록 제한
- 모든 getSample 함수들

### 테스트 결과
```bash
# 요청
curl 'http://localhost:10804/api/tracking/statistics/supplement?startDate=2025-11-24&endDate=2025-11-30'

# 응답 (수정 전)
{
  "data": {
    "startDate": "2025-11-24",
    "supplements": [{
      "dailyStats": [
        { "date": "2025-11-23" }  // ❌ 하루 전
      ]
    }]
  }
}

# 응답 (수정 후)
{
  "data": {
    "startDate": "2025-11-24",
    "supplements": [{
      "dailyStats": [
        { "date": "2025-11-24" }  // ✅ 정확
      ]
    }]
  }
}
```

---

## 📌 주요 변경 파일

### 1. 영양제 기록 정렬 & maxCount
**파일:** `src/tracking/services/records.service.ts`
- transformSupplementRecords() 함수에 userId 추가 및 display_order 정렬
- getRecordLimit() SUPPLEMENT maxCount 10으로 수정

### 2. 영양제 통계 매트릭스 구조
**파일:** `src/tracking/dto/statistics/statistics.dto.ts`
- DailySupplementStatusDto 추가
- SupplementWeeklyStatsDto 추가
- NutrientIntakeDto 추가 (percentage 필드 제거)
- SupplementStatisticsDto에 nutrients 필드 추가

**파일:** `src/tracking/services/statistics.service.ts`
- getSupplementStatistics() 완전 재구현 (매트릭스 구조)
- calculateNutrientIntake() 함수 추가 (영양소 섭취 통계)
- getSampleSupplementStatistics() 수정

**파일:** `src/tracking/controllers/statistics.controller.ts`
- getSupplementStatistics() 엔드포인트 수정 (startDate, endDate 파라미터 추가)

### 3. KST 타임존 수정
**파일:** `src/tracking/services/statistics.service.ts`
- formatKoreanDate() 유틸 함수 import
- 모든 toISOString().split('T')[0] 제거 (14곳)
- 모든 통계 함수의 날짜 처리 수정

**파일:** `src/common/utils/korea-date.util.ts`
- formatKoreanDate() 유틸 함수 활용

---

## ✅ 테스트 완료
- [x] 영양제 기록 조회 정렬 순서 확인 (display_order 기준)
- [x] 영양제 기록 maxCount 확인 (10)
- [x] 영양제 통계 매트릭스 API 동작 확인
- [x] 1그룹 영양제 7일 데이터 보장 확인
- [x] 2,3그룹 영양제 null 처리 확인
- [x] display_order 정렬 확인
- [x] 영양소 섭취 통계 계산 로직 확인 (실제 먹은 횟수 기준)
- [x] 하루 권장량 초과 방지 확인 (Math.min)
- [x] 영양소 가나다 순 정렬 확인
- [x] percentage 필드 제거 확인
- [x] KST 타임존 처리 확인 (모든 통계 API)
- [x] 날짜 응답 정확성 확인 (startDate=2025-11-24 → date=2025-11-24)

---

## 🎯 핵심 성과
1. **복잡한 매트릭스 데이터 구조 설계 및 구현** - 7일간 영양제 섭취 상태를 null 처리를 포함한 2D 배열로 표현
2. **정밀한 영양소 계산 로직 구현** - 실제 섭취 횟수 기반, 하루 권장량 초과 방지
3. **전체 통계 시스템 KST 타임존 통일** - 14개 함수 수정으로 날짜 일관성 확보

---

## 📝 비고
- 이번 작업은 프론트엔드 UI 요구사항에 맞춘 정밀한 데이터 구조 설계가 핵심
- 영양소 계산 로직은 비즈니스 규칙(하루 권장량 초과 불가)을 정확히 반영
- KST 타임존 문제는 기존 유틸 함수(formatKoreanDate)를 활용하여 일관성 있게 해결
