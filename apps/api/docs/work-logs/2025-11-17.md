# 작업일지 - 2025년 11월 17일

## 📋 작업 내용

### 1. 테이블 리네이밍: mission_attempts → user_missions
**목적**: 테이블명의 직관성 개선 (주체인 '사용자' 명시)

#### 변경 사항
- **DB 테이블명**: `mission_attempts` → `user_missions`
- **Prisma 모델명**: `MissionAttempt` → `UserMission`
- **@@map 지시자**: `@@map("mission_attempts")` → `@@map("user_missions")`

#### 영향받은 파일
- `prisma/schema.prisma` - 모델명 및 테이블 매핑 변경
- `src/common/services/prisma.service.ts` - getter 메서드 `missionAttempt` → `userMission`
- `src/mission/mission-completion.service.ts` - 모든 `tx.missionAttempt`, `this.prisma.missionAttempt` 참조 업데이트
- `src/quiz/quiz-completion.service.ts` - `tx.missionAttempt` 참조 업데이트
- `src/management/services/management-mission.service.ts` - `this.prisma.missionAttempt` 참조 업데이트

#### 주의사항
- **Prisma 워크플로우 준수 필요**: schema.prisma 수정 → `prisma migrate dev` 순서
- DB를 먼저 수정하면 Prisma와 불일치 발생 가능

---

### 2. 과거 미션 수행 로직 개선
**목적**: 놓친 미션을 나중에라도 수행할 수 있도록 허용 (단, 포인트 0점)

#### 변경된 로직
```typescript
// 기존: 당일 미션만 수행 가능
if (challengeMission.day !== currentDay) {
  throw new BadRequestException('오늘의 미션이 아닙니다');
}

// 변경: 과거 미션 허용, 미래 미션만 차단
if (challengeMission.day > currentDay) {
  throw new BadRequestException('아직 수행할 수 없는 미션입니다');
}

// 포인트 지급 조건 추가
const isOnScheduledDay = challengeMission.day === currentDay;
const pointsEarned = (isCompleted && isOnScheduledDay) ? challengeMission.points : 0;
```

#### 적용 파일
- `src/mission/mission-completion.service.ts` (lines 85-88, 270-272)

---

### 3. 새로운 API 추가

#### 3-1. 완료한 미션 목록 조회
**엔드포인트**: `GET /api/missions/completed`

**파라미터**:
- `startDate` (required): 시작일 (YYYY-MM-DD)
- `endDate` (required): 종료일 (YYYY-MM-DD)

**응답 예시**:
```json
{
  "success": true,
  "data": {
    "dateRange": { "startDate": "2025-01-01", "endDate": "2025-01-10" },
    "dayRange": { "startDay": 1, "endDay": 10 },
    "missions": {
      "1": [
        {
          "challengeMissionId": 123,
          "missionName": "자기선언문 작성",
          "pointsEarned": 100,
          "completedAt": "2025-01-15T10:30:00Z"
        }
      ]
    },
    "totalCount": 15
  }
}
```

#### 3-2. 놓친 미션 목록 조회
**엔드포인트**: `GET /api/missions/missed`

**파라미터**:
- `startDate` (required): 시작일 (YYYY-MM-DD)
- `endDate` (required): 종료일 (YYYY-MM-DD)

**응답 예시**:
```json
{
  "success": true,
  "data": {
    "dateRange": { "startDate": "2025-01-01", "endDate": "2025-01-10" },
    "dayRange": { "startDay": 1, "endDay": 9 },
    "missions": {
      "2": [
        {
          "challengeMissionId": 124,
          "missionName": "물 2L 마시기",
          "originalPoints": 50,
          "currentPoints": 0
        }
      ]
    },
    "totalCount": 5,
    "message": "과거 미션만 조회됩니다"
  }
}
```

**특징**:
- 미래 미션은 자동 제외 (currentDay - 1까지만 조회)
- 놓친 미션은 지금 수행해도 0점

#### 3-3. 자기선언문/칭찬내역 조회
**엔드포인트**: `GET /api/missions/self-records`

**파라미터**: 없음 (활성화된 챌린지 기준)

**응답 예시**:
```json
{
  "success": true,
  "data": [
    {
      "type": "DECLARATION",
      "day": 1,
      "contents": "자기 선언문 내용이 들어갑니다.",
      "completedAt": "2025-01-15T10:30:00Z",
      "pointsEarned": 100
    },
    {
      "type": "SELF_PRAISE",
      "day": 10,
      "contents": "칭찬 내용이 들어갑니다.",
      "completedAt": "2025-01-24T14:20:00Z",
      "pointsEarned": 100
    }
  ]
}
```

**구현 로직**:
1. `missions` 테이블에서 `record_type IN ('DECLARATION', 'SELF_PRAISE')` 조회
2. `challenge_missions`에서 해당 미션의 `challengeMissionId` 획득
3. `user_missions`에서 `metadata.contents` 추출
4. day 순서대로 정렬 반환

**적용 파일**:
- `src/mission/mission-completion.service.ts` - `getCompletedMissions()`, `getMissedMissions()`, `getRecords()` 메서드 추가
- `src/mission/mission.controller.ts` - 3개 엔드포인트 추가

---

## 🐛 발생한 문제 및 해결

### 문제 1: Prisma Client 타입 불일치
**증상**: `Property 'userMission' does not exist on type 'PrismaService'`

**원인**:
- DB는 `user_missions` 테이블로 변경됨
- Prisma schema는 수정되었으나 Client가 재생성되지 않음
- PrismaService getter가 업데이트되지 않음

**해결**:
1. `prisma/schema.prisma` 재확인 및 수정
2. `npx prisma generate` 실행
3. `src/common/services/prisma.service.ts`에서 `get userMission()` 추가
4. 모든 서비스 파일에서 `missionAttempt` → `userMission` 변경

### 문제 2: 엔드포인트 명명 모호성
**증상**: `/api/missions/records`가 모든 미션 기록을 조회하는 것처럼 보임

**해결**: `/api/missions/self-records`로 변경하여 자기선언문/칭찬내역만 조회함을 명확히 함

---

## 📝 학습 내용

### Prisma 마이그레이션 올바른 순서
```bash
# ✅ 올바른 방법
1. schema.prisma 수정
2. npx prisma migrate dev --name migration_name
3. 코드에서 사용

# ❌ 잘못된 방법 (오늘 실수한 방법)
1. DB에서 직접 SQL 실행
2. schema.prisma 수정
3. npx prisma generate (또는 db pull)
→ DB와 스키마 불일치로 혼선 발생
```

**교훈**: Prisma를 사용할 때는 항상 "schema.prisma가 진실의 원천(single source of truth)"

---

## ✅ 테스트 결과

### 서버 상태
- **포트**: 10804
- **상태**: 정상 실행 중 ✅
- **빌드 에러**: 0개
- **새로운 엔드포인트**:
  - `GET /api/missions/completed` ✅
  - `GET /api/missions/missed` ✅
  - `GET /api/missions/self-records` ✅

### API 문서
- Swagger: http://localhost:10804/api/docs
- 모든 엔드포인트 정상 등록 확인

---

## 📌 후속 작업 필요 사항

1. **실제 데이터로 API 테스트**
   - 자기선언문/칭찬내역이 있는 사용자로 테스트
   - 과거 미션 완료 시 포인트 0점 지급 확인
   - 미래 미션 수행 차단 확인

2. **프론트엔드 연동**
   - 놓친 미션 목록 UI 구현
   - 자기선언문/칭찬내역 조회 화면 연동

---

## 🎯 완료 항목
- [x] `mission_attempts` → `user_missions` 테이블 리네이밍
- [x] 과거 미션 수행 허용 (0점)
- [x] 완료한 미션 조회 API
- [x] 놓친 미션 조회 API
- [x] 자기선언문/칭찬내역 조회 API
- [x] Prisma schema 및 모든 참조 파일 업데이트
- [x] 빌드 에러 해결
- [x] 서버 정상 실행 확인

---

## 🚨 추가 수정 사항 (오후 작업)

### 4. Prisma Schema 대규모 수정
**시간**: 2025-11-17 오후 5시 ~ 6시

#### 4-1. User 모델 필드 추가
**목적**: 휴대폰 인증 회원가입 지원

**추가된 필드**:
```prisma
birthDate   String?  @map("birth_date") @db.VarChar(8)
sex         String?  @db.VarChar(2)
localCode   String?  @map("local_code") @db.VarChar(2)
telecom     String?  @db.VarChar(10)
```

**email, password nullable 변경**:
```prisma
// 기존
email     String  @unique
password  String

// 변경 후
email     String?
password  String?
```

**이유**: 휴대폰 인증 회원가입 시 email, password 없이 가입 가능하도록 수정

#### 4-2. PhoneVerificationLog 모델 수정
**변경 사항**:
- `phoneNumber` → `mobile` 필드명 변경 (DB 컬럼명과 일치)
- `certNumber`에 `@unique` 제약조건 추가 (findUnique 사용 가능)

```prisma
// 기존
certNumber  String    @map("cert_number") @db.VarChar(50)
phoneNumber String    @map("phone_number") @db.VarChar(11)

// 변경 후
certNumber  String    @unique @map("cert_number") @db.VarChar(50)
mobile      String    @db.VarChar(255)
```

### 5. 챌린지 활성화 플로우 완전 수정
**목적**: PENDING → ACTIVE 정상 플로우 구현

#### 5-1. activate API 수정
**변경 전**: ACTIVE 상태로 즉시 생성
**변경 후**: PENDING 상태로 생성

```typescript
// 변경 전
status: 'ACTIVE',
currentDay: 1,
// DailyProgress 생성
// User 상태 CHALLENGER로 변경

// 변경 후
status: 'PENDING',
// currentDay, DailyProgress, User 상태 변경 없음
```

#### 5-2. start-date API 수정
**변경 전**: 새로운 UserChallenge 생성 (CREATE)
**변경 후**: 기존 PENDING UserChallenge 업데이트 (UPDATE)

```typescript
// PENDING 챌린지 찾기
const pendingChallenge = await tx.userChallenge.findFirst({
  where: { ticketId: ticket.id, status: 'PENDING' }
});

// UPDATE로 변경
await tx.userChallenge.update({
  where: { id: pendingChallenge.id },
  data: { startDate, endDate, expiresAt }
});
```

#### 5-3. 챌린지 활성화 크론잡 추가
**새 파일**: `src/challenge/challenge-scheduler.service.ts`

**기능**:
- 매일 00:00 (KST) 실행
- PENDING 챌린지 중 오늘이 시작일인 것들을 ACTIVE로 전환
- DailyProgress(Day 1) 생성
- User 상태를 CHALLENGER로 변경

**크론 설정**:
```typescript
@Cron('0 0 * * *', {
  name: 'activate-challenges',
  timeZone: 'Asia/Seoul',
})
```

**테스트용 수동 실행 API**:
```
POST /api/challenges/scheduler/run-activation
```

### 6. 날짜/시간 처리 개선
**파일**: `src/challenge/utils/challenge-date.util.ts`

#### formatDateToString 수정
**문제**: UTC 메서드 사용으로 날짜 시프트 발생
**해결**: 로컬 시간대 메서드 사용

```typescript
// 변경 전
const year = date.getUTCFullYear();
const month = String(date.getUTCMonth() + 1).padStart(2, '0');
const day = String(date.getUTCDate()).padStart(2, '0');

// 변경 후 (KST 기준)
const year = date.getFullYear();
const month = String(date.getMonth() + 1).padStart(2, '0');
const day = String(date.getDate()).padStart(2, '0');
```

### 7. 놓친 미션 로직 수정
**파일**: `src/mission/mission-completion.service.ts`

**문제**: 오늘 미완료 미션이 표시되지 않음
**원인**: `adjustedEndDay = Math.min(endDay, currentDay - 1)` (오늘 제외)

**해결**:
```typescript
// 변경 전
const adjustedEndDay = Math.min(endDay, currentDay - 1);

// 변경 후
const adjustedEndDay = Math.min(endDay, currentDay);
```

---

## 🐛 발생한 문제 및 해결 (오후)

### 문제 3: User 모델 birthDate 필드 누락
**증상**:
```
Unknown field `birthDate` for select statement on model `User`
```

**원인**: schema.prisma에 birthDate, sex, localCode, telecom 필드 누락

**해결**: User 모델에 4개 필드 추가

### 문제 4: PhoneVerificationLog phoneNumber 컬럼 불일치
**증상**:
```
The table `public.phone_verification_logs` does not exist in the current database.
Argument `phoneNumber` is missing.
```

**원인**:
- schema.prisma는 `phoneNumber` 사용
- 실제 DB 컬럼은 `mobile`

**해결**: schema.prisma에서 `phoneNumber` → `mobile`로 통일

### 문제 5: certNumber로 findUnique 불가
**증상**:
```
Argument `where` of type PhoneVerificationLogWhereUniqueInput needs at least one of `id` arguments.
```

**원인**: certNumber 필드에 `@unique` 제약조건 없음

**해결**: `certNumber String @unique` 추가

### 문제 6: email unique 제약으로 인한 null 충돌
**증상**:
```
P2032: Prisma Client could not process a value in the result.
Field: "email", expected_type: "String", found: "null"
```

**원인**:
- email 필드가 `String @unique`
- 휴대폰 회원가입 시 email이 null
- unique 제약으로 인해 null 값 충돌

**해결**:
- email을 nullable로 변경: `String?`
- @unique 제약 제거

### 문제 7: 서버 다중 실행
**증상**: 포트 10804가 이미 사용 중인데도 새 서버가 계속 실행됨

**원인**: `npm run start:dev` 실패 시에도 프로세스가 종료되지 않음

**해결**:
- 서버 실행 전 포트 체크 필수
- 백그라운드 서버 실행 최소화

---

## 🚀 GKE 배포

### 배포 이미지
- **이전**: `asia-northeast3-docker.pkg.dev/api-dev-biocom/biocom-api/biocom-api:20251117155914`
- **현재**: 형님이 배포 중

### 배포 필수 사항
1. Docker 이미지 빌드 시 자동으로 `npx prisma generate` 실행됨
2. schema.prisma 변경사항이 자동 반영됨
3. 새로운 이미지로 배포 필요

---

## 📌 안정 버전 (롤백 포인트)

### ✅ 커밋 ID: `4475ed6`
**날짜**: 2025-11-17 오후 5:43 (KST)

**커밋 메시지**: "fix: User 모델 email, password 필드 nullable로 변경 및 unique 제약 제거"

**포함 사항**:
- User 모델: birthDate, sex, localCode, telecom 필드 추가
- User 모델: email, password nullable 변경 및 unique 제약 제거
- PhoneVerificationLog: phoneNumber → mobile 변경
- PhoneVerificationLog: certNumber @unique 추가
- 챌린지 활성화 플로우 완전 수정 (PENDING → ACTIVE)
- 챌린지 활성화 크론잡 추가
- 날짜/시간 처리 개선
- 놓친 미션 로직 수정

**롤백 방법**:
```bash
git checkout 4475ed6
```

**비고**:
- 이 버전은 휴대폰 인증 회원가입/로그인 시스템이 완전히 구현된 상태
- Prisma schema와 코드가 완전히 동기화된 상태
- GKE 배포 전 최종 검증 완료 버전

---

## 📝 최종 Git 커밋 이력

### 커밋 1: `e7acf00` (오후 5:30)
**메시지**: "fix: Prisma schema 수정 - User 테이블 및 PhoneVerificationLog 필드 추가"
- schema.prisma만 수정

### 커밋 2: `34f45cc` (오후 5:35)
**메시지**: "fix: 휴대폰 인증 및 챌린지 시스템 완전 수정"
- 전체 파일 수정 (10개 파일, 328줄 추가, 110줄 삭제)
- challenge-scheduler.service.ts 신규 추가

### 커밋 3: `4475ed6` (오후 5:43) ⭐ **안정 버전**
**메시지**: "fix: User 모델 email, password 필드 nullable로 변경 및 unique 제약 제거"
- email, password nullable 변경
- email @unique 제약 제거

### 커밋 4: `6df1412` (오후 6:00)
**메시지**: "docs: 작업일지 업데이트 및 infra-gcp 배포 설정 반영"
- 작업일지 업데이트 (오후 작업 내용 정리)
- infra-gcp/k8s/deployment.yaml 최신화

---

## ✅ 최종 완료 상태

### 오늘 완료된 작업
1. ✅ mission_attempts → user_missions 테이블 리네이밍
2. ✅ 과거 미션 수행 로직 개선 (0점 지급)
3. ✅ 완료한 미션 조회 API
4. ✅ 놓친 미션 조회 API
5. ✅ 자기선언문/칭찬내역 조회 API
6. ✅ User 모델 필드 추가 (birthDate, sex, localCode, telecom)
7. ✅ User 모델 email/password nullable 변경
8. ✅ PhoneVerificationLog 모델 수정
9. ✅ 챌린지 활성화 플로우 완전 수정 (PENDING → ACTIVE)
10. ✅ 챌린지 활성화 크론잡 구현
11. ✅ 날짜/시간 처리 개선 (KST 기준)
12. ✅ 놓친 미션 로직 수정 (오늘 포함)
13. ✅ 작업일지 작성 및 안정 버전 기록

### 배포 대기 중
- **커밋**: `6df1412` (최신)
- **안정 버전**: `4475ed6` (롤백 포인트)
- **배포 환경**: GKE (Google Kubernetes Engine)
- **배포 담당**: 형님

### 다음 작업
- GKE 배포 후 실제 데이터로 전체 플로우 테스트
- 휴대폰 인증 회원가입/로그인 실제 테스트
- 챌린지 활성화 크론잡 모니터링 (매일 00:00 KST)
