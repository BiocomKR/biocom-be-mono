/**
 * PG Provider Enum
 * 결제 대행사 구분
 *
 * 새 PG사 추가 시 여기에 enum 값 추가
 * - TOSS: 토스페이먼츠
 * - NAVER: 네이버페이 (예정)
 * - KAKAO: 카카오페이 (예정)
 */
export enum PgProvider {
  TOSS = 'TOSS',
  // NAVER = 'NAVER',
  // KAKAO = 'KAKAO',
}
